# Промпт для Гермеса: писать SEO-статьи недели 8–14 июля

Отдавай Гермесу этот файл + указывай **день**. Одна страница за прогон (YMYL — всплески тонкого контента караются). Вся фактура — только из `data/*.json`, ничего не выдумывать.

## Что делать за прогон

1. Определи страницу дня по таблице в `docs/tz-articles-2026-07-08.md` (раздел «План недели»). Открой её детальный раздел (П1…П7) — там черновики title/H1/description, ключи с частотностью, H2-план, готовые FAQ, вся фактура с датами и **ловушки данных** (читать обязательно — по Рязани нет ударов в strikes.json, Куйбышевский ≠ Новокуйбышевский, битый юникод у Омска и т.д.).
2. Перед стартом прочитай целиком `docs/seo-playbook.md` и сделай grep своих ключей по `data/seo-topics.jsonl` — пересечение ≥40% = тема занята, страницу не создавать.
3. Собери страницу по эталону:
   - **Объектные `/npz/*`** — по `npz/omskij-npz.html` (fact-grid, timeline-list, status-grid, faq-list, news-cta).
   - **Региональные / объяснялки** — по `crimea.html` (landing-hero, station-list, tl-entry, link-grid).
   Только готовые классы, никаких инлайн-стилей.
4. Пройди **чек-лист приёмки** из ТЗ (он обязателен для каждой страницы) — title 50–60, один H1, 3–6 H2, FAQ, 3 JSON-LD + для /npz ещё Place/Organization с гео, self-canonical, дисклеймер OSINT, ≥3 внутренние ссылки.
5. В **том же коммите**: строка в `data/seo-topics.jsonl` (готовые строки — внизу ТЗ) + запись в `sitemap.xml` (lastmod = сегодня; priority: объект 0.7, регион/объяснялка 0.8).
6. Публикация (см. «Доставка и деплой» ниже).

## ⚠️ Обязательные правки, вскрытые 08.07 (эталон их НЕ содержит или содержит с багом)

1. **BreadcrumbList для /npz/*: уровень 2 = «НПЗ России» → `/refineries`** (в эталоне omskij оба уровня вели на `/` — НЕ повторять; в moskovskij уже правильно).
2. **Навбар: ссылка Крыма = `/crimea`, НЕ `/krym`** (`/krym` отдаёт 404; исправлено в эталоне 08.07, но перепроверяй в копии).
3. **Входящие ссылки обязательны — иначе страница оторвана от сайта.** После создания страницы добавь на неё ссылки:
   - `/refineries`: сделай ссылкой название завода в таблице `refinery-table` + в списке «Крупнейшие НПЗ» (если завод там есть);
   - хаб-карточка: `/deficit` (для НПЗ и регионов) или `/crimea` (для крымских тем) — по образцу карточки Московского/Омского НПЗ в `link-grid`;
   - обратная ссылка из ближайшей ежедневной сводки `/news/<дата>`, если завод в ней упомянут.
   Эти правки идут отдельными строками в том же дне (частично это П7, но для достижимости делаем сразу).
4. **Валидируй JSON-LD** через `python3 -c "import json; ..."` перед коммитом — три-четыре блока должны парситься.

## Обложки — ТОЛЬКО через Codex

Обложка сводки (город удара + подпись, едина для сайта `/news` и Telegram) генерируется **только Codex**: `python3 hermes/scripts/build-covers.py` (внутри `codex exec image_gen`, img2img по реальному фото события). НЕ рисовать обложку самому и НЕ звать другую модель. PIL-фолбэк `agents/gen_cover_today.py` — автоматический бэкстоп: если Codex недоступен (429/кредиты/не ответил), обложка генерится им, чтобы пост вышел с картинкой (выпуск не блокируем). В отчёте пометить, что обложка — фолбэк, для последующей перегенерации через Codex. Оператор дал стоячее разрешение вызывать Codex для обложек без переспроса.

## Доставка и деплой

- Живой репо — **анонимный** `volobuevaleksand7-hue/npz-tactical-map`. Push идёт с VPS (deploy-key `github-npznew`). Перед push: `git pull --rebase --autostash`. Один коммит = одна страница. Не трогать защищённые pre-commit хуком: `index.html / styles.css / app.js / radar.html / version.json / CHANGELOG.md`.
- **⚠️ Авто-деплой (GitHub Actions → Vercel) сломан с 08.07** (протух `VERCEL_TOKEN`). Пуш в main НЕ выкатывает сайт. Пока токен не обновят — после push страница в прод не выйдет сама. Деплой делает оператор вручную (`vercel --prod` с машины, где залогинен sergeyramas) ЛИБО жди починки токена. Гермес: после push отметь в отчёте «задеплоить вручную», не считай задачу закрытой до появления страницы на https://npz-tactical-map.vercel.app/<url>.

## Порядок недели (из ТЗ)

| День | Страница | Раздел ТЗ |
|---|---|---|
| Ср 08.07 | `/npz/moskovskij-npz` | П1 — ✅ сделано 08.07 |
| Чт 09.07 | `/talony` | П2 |
| Пт 10.07 | `/moskva` | П3 |
| Сб 11.07 | `/npz/slavneft-yanos` | П4 |
| Вс 12.07 | `/npz/kujbyshevskij-npz` | П5 |
| Пн 13.07 | `/npz/ryazanskij-npz` | П6 |
| Вт 14.07 | реоптимизация `/attacks`,`/refineries`,`/crimea` | П7 |
