# AGENT_ACTIVITY — npz-tactical-map

Координация агентов по проекту. Одна строка на значимое действие.

## Зоны ответственности (чтобы агенты не пересекались по файлам)
- **agent:npz-status** → `data/fuel-state.json`: `refineries[]`, `export_terminals[]`, часть `events[]`, `meta`.
- **agent:fuel-market** → `data/fuel-state.json`: `deficit_regions[]`, `national_balance`, `fuel_balance`, часть `events[]`, `meta`.
- **Hermes/VPS routines** → `data/*.json`, `news.html`, `news/*.html`, `sitemap.xml`, `assets/cover-*.png`, Telegram pipeline.
- **UI-shell protected** → `index.html`, `styles.css`, `app.js`, `radar.html`, `version.json`, `CHANGELOG.md`, `.vercelignore`.
  - Эти файлы трогает только человек/основной агент по явному запросу.
  - Любой commit с UI-shell файлами требует `ALLOW_FRONTEND_RELEASE=1`, SemVer и запись в этот журнал.
  - Hermes не делает прямой `vercel deploy --prod` для оболочки; источник правды — `origin/main`.

## Log
- 2026-07-13 — Codex/Mac (уточнение владельца): `v1.20.2` **акцент на риске атак в резервной плашке.** Фраза «сайт подвергается атакам» выделена тёмно-красным жирным маркером на белом фоне; компактная рамка, CTA и сценарий закрытия не менялись. UI-shell release, `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-13 — Codex/Mac (уточнение владельца по скриншоту): `v1.20.1` **сдержанный вид резервного Telegram-баннера.** Красная заливка заменена белым фоном; плашка стала тоньше, с ограниченной шириной и видимой красной рамкой по периметру. Свечение сохранено, но ослаблено. Текст, CTA, ссылка и закрытие на 14 дней не менялись. UI-shell release, `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-13 — Codex/Mac (запрос владельца): `v1.20.0` **резервный Telegram-канал на главной карте.** Сразу под OSINT status strip добавлена красная пульсирующая плашка с утверждённым текстом о атаках, CTA «Сохранить доступ» → `https://t.me/bplalarm` и крестиком. Закрытие сохраняется в `localStorage` на 14 дней; эффект затрагивает только фон/свечение, а `prefers-reduced-motion` отключает анимацию. Старый `sub-nudge.js` отключён на главной, поэтому нет второго отложенного попапа. Добавлена статическая регрессионная проверка `scripts/test-subscription-alert.sh`. UI-shell release, `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-11 ~16:25 МСК — Opus/Mac (запрос владельца): **сторож дневной сводки + инбокс инцидентов Гермесу.** `agents/summary-watchdog.py` (VPS cron 8:15/20:15 МСК) проверяет живой `/news`: есть ли карточка за сегодня и не на заглушке ли обложка (ловит `card-missing` и `cover-fallback` — гонку cover→news из фикса 11.07). При провале: пинг владельцу + **OPEN-инцидент в `docs/agents/incidents.md`** с конкретными шагами → коммит через git-sync → Гермес подхватывает на heartbeat (новый шаг **HERMES.md §0.1a**: читать инбокс, чинить OPEN, ставить RESOLVED). Сторож сам авто-резолвит инцидент, когда сводка появляется. Docs+agents-only, UI-shell не тронут.
- 2026-07-10 — Opus/Mac (запрос владельца «отдели карту АЗС»): `v1.15.0` **карта АЗС → отдельная страница `/karta-azs`.** Проблема: карта заправок была только вкладкой SPA на `/` → Метрика/Vercel видели всё как `/`, нет индексируемой посадочной под «карта азс». Решение по образцу `/radar`, но БЕЗ переписывания карты: `karta-azs.html` = каркас `index.html` + свой SEO-`<head>`/`<h1>` + инлайн `history.replaceState(…,"#azs")` → грузится во вкладку АЗС через существующий deep-link `app.js:1814` (тот же `app.js`, ноль дублей). Регистрация в `data/seo-topics.jsonl` (tool, «карта азс») → sitemap + меню всех страниц (`build-nav.py` LABELS `⛽ Карта АЗС`). 2 фоновых агента: (1) релинк всех `/?view=azs`→`/karta-azs` в источниках — ручные `karta-benzina-krym`/`crimea`/`radar`/`zakrytye-azs`, шаблон `gen-news.py` (news.html+57 сводок), Telegram `broadcast.py`; (2) аудит остальных вкладок — history→`/attacks`, forecast→`/crisis` уже покрыты, economy = нулевой спрос, страниц не надо. Свёл на свежий `origin/main` (был -25 от Гермеса): reset + reapply source-патча + регенерация пайплайна (`gen-news.py`→build-nav/sitemap/rss). Проверено: 0 `view=azs` в HTML, `check-ia.py` ✓ (32 live), `/karta-azs` в sitemap. Тронул shell (`index.html`,`radar.html`), `sw.js` v10→v11, генераторы, реестр, 90+ регенеренных страниц. `ALLOW_FRONTEND_RELEASE=1`. ⚠️ `karta-azs.html` — клон body `index.html`, при структурных правках SPA пере-синкать (в файле ponytail-коммент). Хвост: воронки `azs-pages.py` (gde-est-benzin/benzin-na-trasse/gde-dizel) ещё CTA-ят на `/?view=azs` через генератор — перенаправить отдельно.
- 2026-07-10 ~09:00 МСК — Opus/Mac (запрос владельца): **SEO-аналитик = 2 глаза.** Добавил в `BRIEF_analyst.md` глаз качества трафика (Метрика: отказы/глубина/время/цели) + петлю «статья выстрелила → в бэклог на углубление кластера → разведчик копает». Обновил PLAN.md (KPI+таблица). Docs-only.
- 2026-07-10 — Opus/Mac (follow-up к замечанию #2 ревью MiMo из v1.13.0): `v1.13.1` **клик-фиксация выпадашки «Аналитика» на лендингах.** На главной фикс уже был (v1.13.0), но на статьях `news-nav` дропдаун открывался только `:hover` → на тач-планшетах в ландшафте (>768, где мобильный `<=768` клик-тоггл не ловил) меню и вложенный аккордеон «🚀 Ракетная опасность» было не открыть. Разведка: инлайн-JS дропдауна дрейфовал в **4 копии** по 30 страницам, а `/news/*` и `/npz/*` (~90 стр.) были **вовсе без JS** (hover-only) — вариант help.html вдобавок мешал в тот же `<script>` регистрацию SW + тему `npz-theme`. Решение (option a — централизация, убивает дрейф + чинит и бес-JS сводки): новый `/nav-dropdown.js` (клик-тоггл `.nav-dropdown.open` на всех вьюпортах + клик-вне/`Esc`, зеркалит `.tab-dropdown.open`); `build-nav.py` срезает старые инлайн-конструкции регэкспом (тему/SW НЕ трогает — проверено на help.html), линкует shared-скрипт во все 86 лендингов и штампует `?v` (ASSET_RE расширен). `build_menu` добавляет пункт «📊 Все статьи · каталог →» (т.к. `preventDefault` на «Аналитика ▾» снимает переход на `/analytics`). CSS `news.css`: `.nav-dropdown.open .nav-dropdown-menu{display:block}` + `.nav-drop-all`. Генераторы `gen-rocket-danger.py`/`gen-fuel-pages.py` перестали эмитить инлайн-дропдаун. Проверено в браузере на **1024×768 (тач-ландшафт >768)**, синтетический click (не hover): tap открывает `.open`+`display:block`, аккордеон «Ракетная опасность» разворачивает города, `Esc`/клик-вне закрывают, двойной tap → закрыто; ноль console-ошибок; скриншот меню открытым. `check-ia.py` ✓ (28 live). `py_compile` ✓. SW-кеш v8→v9. Тронул только shell/build/CSS/генераторы (данные/Гермес не задеты). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-10 — Opus/Mac (запрос владельца «выпадашка Аналитика не фиксируется + ракетные опасности плохо смотрятся, сгруппировать; места не хватит под все статьи»): `v1.13.0` **сгруппированная выпадашка «Аналитика» + клик-фиксация.** Диагноз: меню было pure-CSS `:hover` с мёртвой зоной `top:calc(100%+6px)` → схлопывалось при движении мыши; главная-дропдаун рендерился ПЛОСКО (23 пункта, 8 из них — город-строки «Ракетная опасность»). Фиксы в `build-nav.py` (единый источник → 3 поверхности): GROUPS переведены на предикаты + collapse-флаг, ракетные вынесены в свёрнутый `<details>`; главная-дропдаун теперь с заголовками групп + `INDEX-MENU` маркеры (regex по `<div>` ломался на вложенных `</div>`); `cover_for()` берёт обложку по конвенции имени с проверкой существования (убрал хардкод 20 путей); `stamp_assets()` штампует `?v=<md5[:8]>` на styles.css/app.js в index.html — **найден латентный stale-shell баг** (index линковал ассеты без `?v`, лендинги — с ним); index-drawer «Аналитика» переведён на `DRAWER-ANALYTICS` маркеры (был hand-maintained, показывал 1/8 городов). JS дропдауна: mobile-only display-toggle → клик-фиксация `.open` на всех вьюпортах + клик-вне/`Esc`. CSS: `.tab-dropdown.open`/`.tdm-group`/`.tdm-sub`/`.nav-drop-sub`/`.ndp-sub` + `max-height:min(72vh,540px)` скролл. Проверено в claude-in-chrome (открытие/фиксация/Esc/аккордеон desktop+drawer, `max-height:540 scrolls:true`, `?v` в ссылках). Ревью MiMo (`mimo-v2.5-pro`): логика чистая (ни одна страница не выпала из групп, regex/`cover_for`/`stamp_assets` идемпотентны); 3 замечания — #1 no-JS misdiagnosis (preventDefault внутри listener → без JS ссылка живёт), #2 лендинг-дропдауны hover-only на тач-планшетах >768 (pre-existing, отложено), #3 index-drawer дрейф (закрыт — теперь генерится). ⚠️ **Обложки 11 плиток `/analytics` не дорисованы:** Codex `image_gen` → 403 Forbidden (auth/entitlement, не 429); `cover_for` деградирует аккуратно (без битых `<img>`), дорисовка отложена до восстановления доступа. Дерево = 90 файлов, все мои (build-nav регенерация меню+футер на всех лендингах/сводках + 4 источника), чужого/данных нет. SW-кеш v7→v8. `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-10 — Fable5-fixes/Mac (Фаза 0 аудита Fable5, запрос владельца): **атомарная запись критичных стейт-файлов** (audit C3+H9). `update_strikes.py`: `open('strikes.json','w')` без utf-8/tmp → сбой записи на не-UTF-8 локали обнулял ЕДИНСТВЕННЫЙ архив ударов; → tmp+`os.replace`+`encoding='utf-8'` (и чтение utf-8). `hermes/bot/strike_pipeline.py::jsave` → tmp+`os.replace` (паттерн как в `day_state.py`), чтобы обрыв записи не усекал `pipeline-state.json` в `{}` → массовую повторную рассылку молний (уже случалось). Только backend/Hermes, UI-shell не тронут. Чистый worktree origin/main; `py_compile` + функциональный self-check (roundtrip+utf-8+нет .tmp) зелёные. ⚠️ Гермесу нужен `git pull` чтобы фикс доехал до VPS.
- 2026-07-09 ~23:55 МСК — Fable5/Mac (запрос владельца): **SEO-система «агент агентов»** — план запуска `docs/agents/seo/PLAN.md` (роли/модели/недельный цикл/шаги передачи Гермесу-Codex-Gemini) + 6 брифов ролей `docs/agents/seo/BRIEF_*.md` + seed `data/seo-backlog.jsonl`. Docs+data-only, UI-shell не тронут. Собрано в чистом worktree origin/main.
- 2026-07-09 ~16:20 МСК — Opus/Mac (запрос владельца «мобильная версия ужасна, срочно, 50% трафика»): `v1.11.0` **мобильный UX-пасс.** Наконец увидел реальный мобильный вьюпорт: окно claude-in-chrome не сжимается ниже экрана → сделал iframe-харнес 393px + no-store сервер (порт 8099) — внутренний документ = настоящие 393×852. Найдено и исправлено: (1) страйкбар — блок 2×44px закрывал карту → тонкая строка с h-scroll (nowrap/overflow-x, кнопки 38px); (2) `app.js` initMobile авто-открывал панель через 500мс → карта закрыта на каждой загрузке — УБРАЛ (стартует свёрнутой, открывает 📊); (3) свёрнутая панель прячется полностью `translateY(101%)`; (4) TG-баннер line-clamp 2; (5) чистка padding под удалённый нав. SW-кеш v4→v5. ⚠️ app.js содержал ЧУЖУЮ незакоммиченную фичу strike-candidates (др. агент) — застейджил ТОЛЬКО свой хунк через `git apply --cached` (патч по маркеру), чужое не тронул. Проверено на 393px: карта видна целиком, панель collapsed на старте, 📊 toggle, страйкбар scrollW 551>379. Десктоп не задет (@media ≤820/≤400). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 ~13:10 МСК — Opus/Mac (запрос владельца «панель внизу перекрывает кнопки»): `v1.10.9` **сброс кеша SW + панель-кнопка.** Диагноз: владелец видел СТАРЫЙ shell (шапка с ОЦЕНКА/🤝, без ☰, нижний бар из 9 вкладок) — SW отдаёт статику cache-first под `npz-shell-v3`, а v1.10.7/8 меняли styles.css без правки sw.js → новый SW не ставился, CSS не обновлялась. Фикс: `CACHE v3→v4` (форс переустановки, activate чистит v3). Прод уже был корректен (нижний нав убран в v1.10.7, свёрнутый шит прячется строкой 741). Доп: `.mob-fab` опущен в угол (`bottom:14px+safe`, 46px), т.к. нижнего нава нет. Тронул только sw.js+styles.css (+ мета). ⚠️ В дереве были ЧУЖИЕ незакоммиченные правки (app.js, data/strike-candidates.json, .claude/launch.json, docs/agents/TZ_strike-candidates-pipeline.md) — НЕ коммитил, оставил параллельному агенту. `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 ~12:55 МСК — Opus/Mac (запрос владельца, доводка радара): `v1.10.8` **мобильная шапка радара + фикс незакрываемых попапов.** Скрин: справа зелёная плашка «Авто» (☰ уезжал за край), слева обрезан логотип «РАДАР УГРОЗ», нижний попап «О карте тревог» не закрывался. Фиксы (radar.html): `.refresh-badge`+`.sync` скрыты на мобиле → ☰ на их месте; `brand-t` nowrap+ellipsis, `brand-s` скрыт, шапка ужата, кнопки 🌙/☰ 34px. Попапы `<details>` раскрывались вверх (bottom-anchor) → summary уезжал вверх, закрыть нельзя: добавлено закрытие тапом вне/по карте (pointerdown в capture — Leaflet глушит всплытие), Esc, ✕ на раскрытом; один открыт за раз; summary мельче. Проверено вживую (open→outside-tap/Esc закрывают, ✕ рендерится, one-at-a-time async-ок, десктоп цел). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 ~12:00 МСК — Opus/Mac (запрос владельца «сделай гамбургер, аккуратно»): `v1.10.7` **мобильное меню-☰ на карте и радаре.** Жалоба: с телефона нельзя уйти на радар/статьи и вернуться. Корень: на карте мобильный `.tabs` уезжал в нижний бар из 9 пунктов, перекрытый нижним шитом → недоступен; на радаре `.nav{display:none}` при ≤768px → навигации не было вовсе. Фикс: кнопка ☰ в шапке (карта ≤820px, радар ≤768px) + выезжающий справа drawer со всеми разделами (пункты карт на index проксируют в реальные `#tabs`; на радаре — ссылки + deep-link `?view=`, первый пункт «🗺 Россия» = назад на карту). Нижний бар из 9 вкладок убран, шит приспущен к низу. Reveal-анимация: `requestAnimationFrame`→форсированный reflow (rAF не тикает в фоновой вкладке). Десктоп не тронут. Проверено вживую в браузере (open/close по фону/✕/Esc, лок скролла, панель у правого края, `data-view`-прокси переключает карту). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 — `v1.10.4` (Fable 5, завершение цели ТЗ): **финальный Codex-ревью полного релиза v1.10.x** (`git diff 792c8fc..HEAD`, gpt-5.5). 6/7 областей чисто; 1 BUG: `sw.js` install глотал ошибку `addAll` и активировал пустой SW при 404 любого ассета оболочки (сносил старый кэш → офлайн без `/`). Фикс: split CRITICAL (обязан, иначе install reject → старый SW жив) / OPTIONAL (best-effort). Проверено на localhost:8811 (статик без cleanUrls: /radar/install/help→404, SW всё равно active с 5 критичными + иконки). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 ~10:05 МСК — Opus/Mac (запрос владельца): фикс выбора ЛИДА сводки в `gen-news.py::brief_headline` — вёл первым НПЗ в списке (08.07 → подстанция, ложно ref=1) вместо Саратовского НПЗ (confirmed). Трёхуровневая классификация как у обложки. Пересобран архив.
- 2026-07-09 — `v1.10.3` (Fable 5, жалоба «вижу старую версию»): **инфрафикс доставки.** Корень: `sw.js` подпадал под `/(.*).(css|js)`→`max-age=3600`, Vercel edge отдавал старый `sw.js` (`x-vercel-cache:HIT`, на проде висел `npz-shell-v1` при v3 в репо) → браузер не видел новый SW, старый SW отдавал закешированную оболочку v1.9.2. Фикс: `vercel.json` — `sw.js`/`version.json`/`manifest.webmanifest` → `max-age=0, must-revalidate` (правило после `.js`, чтобы перебить). Не UI-shell код, но затрагивает version.json/CHANGELOG → `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 — `v1.10.2` (Fable 5, жалоба владельца): **возврат панелей радара + «Радар»→«Радар БПЛА».** Регрессия v1.10.0: фикс попапов `.map{z-index:auto}` снёс stacking-context карты — при загруженных данных маркеры Leaflet (z:600) всплывали в корень и накрывали panel/feed (z:20); локально не ловилось (радар-данные 404 → пустой markerPane). Откат `.map{z-index:0}` на styles.css+radar.html. Кнопка переименована в 3 навигациях (index.html таб-бар, radar.html навбар, build-nav.py TOP→18 страниц), таб-бар ужат под метку (на 1920 без наложений). Проверено на localhost:8811 (1440/1920): панели/карточки поверх карты, попап автопан, метка влезает. `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 09:33 МСК — Opus/агент (запрос владельца): пересобрал полную сводку /news/2026-07-09 · файлы news*.html, news-archive.json, sitemap.xml
- 2026-07-09 — `v1.10.1` (Fable 5): **патч по Codex-ревью v1.10.0 + нейтральная атрибуция Leaflet** (жалоба владельца: укр. флаг+ссылка в дефолтном префиксе Leaflet 1.9 — заменён на текст «Leaflet», app.js+radar.html). Codex-находки: fp-инвалидация кэша маркеров ударов (правка записи обновляет маркер/попап), ранний beforeinstallprompt (одноразовое событие ловится с загрузки скрипта), /help в SW SHELL (v3), снят aria-modal с немодального шита. `ALLOW_FRONTEND_RELEASE=1`, проверено на localhost:8811.
- 2026-07-09 ~09:1X МСК — Opus 4.8/Mac (прямой запрос владельца): опубликована **утренняя сводка** `/news/2026-07-09` + обложка дня (Тверь-нефтебаза, Codex image_gen). Файлы: `news.html`, `news/*.html`, `sitemap.xml`, `data/news-archive.json`, `assets/cover-2026-07-09.png`. Собрано из ЧИСТОГО клона origin поверх `v1.10.0`; рабочее дерево Мака загрязнено uncommitted правками параллельной сессии — НЕ трогал. ⚠️ Признание: мои публикаторы/scheduled-tasks гоняли `git pull --rebase --autostash` в общем дереве — это «клоббер» из записей выше; перевожу на git-sync-протокол.
- 2026-07-09 — `v1.10.0` (по цели владельца, Fable 5 — оркестратор + 5 субагентов): **юзабилити мобильной/PWA-версии по ТЗ `docs/tz-ui-fixes-2026-07-09.md`** (блоки 1–5, 7; блок 6 был закрыт в v1.9.1/1.9.2). Распараллелено по файлам: radar.html / styles.css / app.js — Claude-субагенты, install.html + help.html — Claude, картинки /install — Codex image_gen (6 шт., правило обложек), index.html/manifest/sw/sitemap/реестр — оркестратор. Ключевое: safe-area+viewport-fit, FAB(z2500) над шитом, landscape и 769–1024 breakpoints, beforeinstallprompt/appinstalled+toast, standalone скрывает кнопку и TG-баннер, `/help` через seo-topics.jsonl→build-nav.py→check-ia.py (18 страниц зелёно), z-index:auto на картах (попапы), tap-зоны 44px, кэш маркеров ударов, a11y шита/модалки. Бонус: фикс инициализации в фоновой вкладке (rAF+setTimeout-фолбэк). Проверено на localhost:8811 (mobile 375/1000px: FAB-тоггл, Escape, попап поверх панелей, вкладки не переполняются, радар без наложений). `ALLOW_FRONTEND_RELEASE=1`.
- 2026-07-09 — handoff (Opus 4.8, по запросу владельца «Гермес не знает про бота»): **передал `@BPLAlert_bot` в канон Гермеса.** Причина разрыва: бот жил в `docs/agents/bpla-alert-bot.md` + этом журнале, но `HERMES.md` (первичный source-of-truth Гермеса) знал только `@NpzFuel_Bot`, а бот-крон был только в live `crontab -l` (я ставил по ssh) и отсутствовал в референсе `hermes/crontab.hermes`. Auto-memory `npz-bpla-alert-bot.md` — на Маке оператора, Гермес её НЕ видит (у Гермеса нет vault/auto-memory; его память = `HERMES.md` + `AGENT_ACTIVITY.md` + `docs/` в репо). Сделано: секция про `@BPLAlert_bot` в `HERMES.md §4` (2-й инстанс poll.py, env, счётчик→NpzFuel, указатель на runbook) + бот-крон вписан в `hermes/crontab.hermes` (чтобы не терялся при переустановке crontab и был виден). Runbook `docs/agents/bpla-alert-bot.md` — без изменений, канонический.
- 2026-07-09 — `v1.9.0` (по запросу владельца, Opus 4.8): **PWA + обложки карточек + попапы.** PWA: `manifest.webmanifest`+`sw.js` (service worker: офлайн-оболочка, `data/`/`api/`/`version.json` не кэшируются)+`install.html` (инструкции iOS/Android/десктоп)+кнопка «📲 Установить приложение · Будь в курсе» в шапке (`index.html`/`styles.css`→`/install`)+иконки 192/512. Push при закрытом приложении — фаза 2 (Web Push+VAPID); пока тревоги БПЛА шлёт `@BPLAlert_bot`. 5 недостающих обложек `/analytics` (Краснодар/Москва/талоны/карта-бензина-Крым/карта-БПЛА) — Codex `image_gen`, 1200×675 (Codex починен через homebrew-бинарь `/opt/homebrew/bin/codex`; npm-обёртка в `~/.local` была битой — отсутствовал `@openai/codex-darwin-arm64`). `app.js`: общий `POPUP_OPTS` (autoPan) — попапы у верхнего края больше не прячутся под панелью фильтров; «Статус с» НПЗ/терминалов/дефицита → `rusDate`. Агенты: DeepSeek-Flash — кластеризация ⚠ БПЛА-маркеров радара (вошло в `1.8.1` параллельной сессии); Claude — попапы; MiMo сорвался (0 файлов) → PWA сделал оркестратор сам. `ALLOW_FRONTEND_RELEASE=1`, проверено на localhost:8799. ⚠️ **Третий клоббер за сессию:** параллельная сессия (bpla/seo/fuel-pages) многократно делает `git pull --rebase --autostash` в общем дереве — сносила мои uncommitted правки `analytics.html`/`index.html` (переприменял и коммитил сразу, чтобы зафиксировать). НУЖНА координация: агентам не гонять `--autostash`-sync в общем рабочем дереве.
- 2026-07-09 — `v1.8.0` (по запросу владельца, Opus 4.8): **UI-аудит AntiGravity** (`AUDIT-2026-07-08-ui-antigravity.md`). Фронт `app.js`/`styles.css` (`index.html` вошёл в 1.7.0). Тикер→fade-карусель; правило русских дат (`rusDate`/`fmtDay`, видимые даты «7 июля 2026»); **Крым наполнен** — заливка полуострова + слой 91 АЗС как на основной + сводка по сети (T20); фиксы `[object Object]`/версия-из-`version.json`/скелетоны (T1-3); зум bottom-right×3 / OSRM-race `requestId` / кластеры хотспотов / единый `.mob-fab` / ARIA / время МСК (T7-14); моб.панель Крыма / `title` НПЗ / fade вкладок / 2 колонки >1200px / «Сеть АЗС» (T4,T15-19); **баланс** — к «35% выбито полностью» (арифм. верно 115.8/335.6) добавлен «недобор ~57% с учётом частично работающих»; **мигающая лампа** баннера тревог. Новое: `agents/validate-crimea.py` (T6), `.github/workflows/smoke.yml` (T21), `docs/agents/crimea-data-refresh.md` (T5, Гермесу — статусы 91 АЗС Крыма). `ALLOW_FRONTEND_RELEASE=1`, проверено на localhost:8799, Claude-ревью зелёное. ⚠️ **Инцидент:** параллельная сессия (@BPLAlert_bot) сделала `git pull --rebase --autostash` в общем дереве и засташила мои `app.js`-правки — восстановлено из `stash@{0}` + сразу commit. Отложено: 5 обложек карточек `/analytics` (Codex `image_gen` недоступен — нет нативного бинарника).
- 2026-07-09 — `v1.7.0` (по запросу владельца, Opus 4.8): **анонимный бот тревог @BPLAlert_bot + плашки подписки.** Второй инстанс того же `hermes/bot/poll.py` под отдельным анон-аккаунтом (публичное лицо ≠ владелец); кода не форкали — своя `NPZ_BOT_DIR=~/.bpla-alert` и токен, `@NpzFuel_Bot` работает параллельно. Правки в `poll.py`: пресеты интервалов → 10м/30м/1ч/2ч/6ч/12ч/24ч (кнопки по 4 в ряд); уведомление владельцу (`NPZ_OWNER_CHAT=8830414911`) на каждый первый `/start` — гейт по env, NpzFuel не затронут. Сводки в канал — через существующий `NPZ_CHANNEL`, нового кода не потребовалось. UI-shell (`ALLOW_FRONTEND_RELEASE=1`): `index.html`+`styles.css` — закрываемый баннер над картой (localStorage `tgAlertClosed`), добавлен ОТДЕЛЬНЫМ элементом под status-strip (UI-SHELL LOCK не тронут); `radar.html` — CTA в подвале ленты. Ведут на `https://t.me/BPLAlert_bot`. Проверено локально: баннер рендерится/закрывается/помнит закрытие, CTA радара на месте, `poll.py`/`poll_bot.py` компилируются. ⚠️ Требует ручных шагов владельца: `/start` боту с аккаунта-владельца (иначе уведомления не дойдут) + создать канал и добавить бота админом. Разворот 2-го инстанса на VPS — инструкция в `docs/agents/`.
- 2026-07-08 — IA/навигация статей (по запросу владельца, Opus 4.8 + Codex-аналитик): **единое дерево статей, генерится из реестра.** Причина — «разнобой»: выпадашка «Аналитика» имела 6 пунктов при 11 live-страницах, `/moskva` и `/talony` были сиротами, навигация разошлась на ~6 вариантов, в корне висели устаревшие дубли сводок. Сделано: `agents/build-nav.py` собирает единый навбар (группы Регионы/Объяснялки/Прогноз/Справочники) из `data/seo-topics.jsonl` по полю `type` во все 12 статей/лендингов; `agents/check-ia.py` валит на оторванных страницах (нашёл+закрыл 6 дыр в sitemap); удалены `2026-07-07.html`/`news-07-07.html` (устаревшие, 301→`/news/2026-07-07`); починена читаемость выпадашки и `.analytics-card` (тот же баг: dark-фолбэки `--card/--border/--text/--muted` → `--surface/--line/--ink/--ink-dim`). Governance: `docs/seo-playbook.md` (+тип `forecast`, §7) и `HERMES.md §5.2`. ⚠️ Hermes: новая статья = строка в `seo-topics.jsonl` + `sitemap.xml` → `build-nav.py` → `check-ia.py` до зелёного. Карточки `/analytics` для /moskva,/talony,/krasnodar — на Codex-обложки (follow-up). Проверено на проде.
- 2026-07-08 — `v1.6.0` (по запросу владельца, Fable 5 + Codex-ревью): **паритет по БПЛА с radar-map.ru.** Данные тянулись верно, рендер прятал: убрано схлопывание городов НПЗ-регионов (побережье Сочи/Анапа/Туапсе/Новороссийск/Керчь снова видно, ~14→~56 точек); добавлен рендер route/sea/direction_flights маркеров; districts свёрнуты в угрозу региона (рол-ап без geojson). `api/radar-state.js` прокидывает districts/route/sea/direction_flights (pvo режется из каждого) + проверка `upstream.ok`. XSS-фиксы (Codex): `esc()` имён городов/полей ударов в попапах и панели, безопасный `href` источника. Отложено осознанно: полигоны районов (нужен GADM-2 geojson). Проверено локально: 56 точек, ПВО не течёт ни из 6 источников, консоль чистая.
- 2026-07-08 — ops (Fable 5): **вариант B ВКЛЮЧЁН на VPS-крон** — строка K `20 * * * * python3 agents/strike-candidates.py && git-sync` (ежечасно :20, аналог strike-confirm), бэкап crontab снят. Тестовый прогон на живой ленте: 200 сообщений, 0 кандидатов (сейчас только тревоги/отбои, реальных прилётов нет), файл валиден, strikes.json не тронут. HERMES.md §1 усилён: явный запрет разрушительного git (`reset --hard`/`checkout .`/`stash`/`pull`) в общем дереве + обязательное чтение AGENT_ACTIVITY перед ЛЮБОЙ правкой (не только UI). Причина — инцидент ниже.
- 2026-07-08 — incident (Fable 5): сторонний локальный агент дёрнул `git reset --hard origin/main` в рабочем дереве, пока v1.6.0-правки radar.html/прокси были готовы но НЕ закоммичены → снёс их (version/CHANGELOG тоже). Восстановлено переналожением из истории + сразу commit/push. НЕ Hermes-на-VPS (там безопасный `git pull --rebase`). Профилактика: коммитить UI-shell сразу; агентам не давать git-команды в общем дереве; правило добавлено в HERMES.md.
- 2026-07-08 — data-pipeline (Fable 5): добавлен `agents/strike-candidates.py` (**вариант B**): ловит в ленте radar-map (`recent_messages`) сообщения об ударах (strike-глагол И цель-объект НПЗ/завод/нефтебаза), геокодит по `cities`, пишет `data/strike-candidates.json` (схема совместима со `strikes.json` + `status:candidate`/`confidence:rumor`), идемпотентно по msg_id+город/дата. НЕ трогает боевой `strikes.json`, НЕ публикует. Активация (по решению владельца): cron-строка на Hermes VPS `python3 agents/strike-candidates.py && git-sync`. self-check зелёный. ⚠️ Codex нашёл смежный баг: `agents/update-radar-state.py` читает несуществующее поле `feed` вместо `recent_messages` — заведён чип.
- 2026-07-08 — `v1.5.2` (уточнение владельца): запрет ПВО = **только метки на карте**, НЕ текст новостей. Откачен фильтр ленты из v1.5.1 — лента показывает сырой текст сообщений как есть (в т.ч. сводки со словом «ПВО»). Метки/иконки/бейджи/легенда/подсветка ПВО на карте остаются убранными, флаг `pvo` режется в прокси. Проверено локально: лента 100 сообщений (1 с ПВО-текстом виден как новость), на карте 0 меток/бейджей ПВО.
- 2026-07-08 — `v1.5.1` (по явному запросу владельца, Fable 5): **ПВО полностью убрано с радара — юридический запрет отмечать позиции/активность ПВО.** Вырезано во всех слоях radar.html (иконка/бейдж/попап/ветка «наблюдение»/легенда/приоритеты/цвета/подсветка; города только-ПВО не рисуются) + `api/radar-state.js` режет флаг `pvo` из городов и регионов (не доходит до фронта) + лента фильтрует сообщения с «ПВО». «ЧИСТО» → класс `.tc-badge.clear`. Проверено локально: 0 видимого ПВО (innerText/лента/легенда/панель), прокси не отдаёт pvo, маркеры угроз живые. ⚠️ ПРАВИЛО НА БУДУЩЕЕ: никогда не добавлять слой/иконку/бейдж ПВО обратно.
- 2026-07-08 — `v1.5.0` (по явному запросу владельца, Fable 5): `/radar` приближен к radar-map.ru. SVG-модельки угроз вместо треугольников (+`safe`-точка вместо фолбэка на дрона), лента Telegram (`recent_messages`) справа, светлая/тёмная тема (дефолт светлая, voyager↔dark_all, восстановлен утерянный функционал v1.4.11), затухание dim-городов (opacity .45). `api/radar-state.js`: прокинуты `recent_messages`/`sources`/`direction_arrows`/`bpla_icon_fade_sec` + `last_event_ts`/`source_text` городов. Фикс: `toggleStrikesLayer` в `window` (был заперт в IIFE — чекбокс «Удары» кидал ReferenceError). Проверено локально dev-прокси end-to-end (обе темы, лента 60 записей, dim-CSS, мобилка).
- 2026-07-07 — `v1.4.16` (по запросу владельца): Яндекс.Вебмастер верификация → МЕТА-ТЕГ анонимного аккаунта (`yandex-verification=9e3bfa52eddd5836`). Файловый метод (v1.4.15) ОТКАЧЕН: `cleanUrls:true` 308-редиректит любой `.html` до rewrites (проверено curl'ом дважды), Яндекс хочет 200 — не проходит. Удалены `.txt`, старый `yandex_3043c11e2e96ee23.html`, rewrite/header. Токен у Яндекса один на все методы, поэтому мета-код = файловому коду. Старый мета `3043c11e2e96ee23` (аккаунт `sergeyramas`) заменён на новый → старый аккаунт теряет привязку. Верифицировать в UI: вкладка **Meta tag** → Verify (мета уже на проде). ⚠️ стале-упоминания старого кода остались в неживых `seo/*`, `docs/agents/gsc-setup.md` — не трогал.
- 2026-07-07 — `v1.4.15` (по запросу владельца): файловая верификация Яндекс.Вебмастера для анонимного аккаунта. `cleanUrls:true` даёт 308 на `.html` (Яндекс хочет 200), поэтому контент в `yandex_9e3bfa52eddd5836.txt` + `rewrites` в `vercel.json` (`/yandex_9e3bfa52eddd5836.html`→`.txt`) + header Content-Type text/html. Старая верификация (мета `3043c11e2e96ee23` + файл `yandex_3043c11e2e96ee23.html`, привязаны к аккаунту `sergeyramas`) ПОКА оставлена — уберу после подтверждения новой.
- 2026-07-07 — `v1.4.14` (по запросу владельца): Яндекс.Метрика переключена на анонимный счётчик id `110490245` (был `110462213`, привязан к аккаунту владельца). Заменена ВСЯ вставка в `index.html` на новый снипет владельца (добавились опции `ssr`, `ecommerce:"dataLayer"`, `referrer`, `url`). `yandex-verification` мета-тег (Вебмастер) НЕ трогал — новый код не давали, старый ещё висит от прежнего аккаунта. `sources.html` содержит только закомментированный шаблон-заглушку (`YANDEX_METRICA_ID`), не боевой — не трогал.
- 2026-07-07 — ops: verify CI — тестовый коммит после переезда репо, чтобы подтвердить зелёный прогон `.github/workflows/deploy.yml` (VERCEL_* секреты) на новом аккаунте.
- 2026-07-07 — ops (по запросу владельца): Telegram-бэкенд СНОВА ВКЛЮЧЁН — токен `~/.npz-bot/token` восстановлен, cron-строки `broadcast.py --briefing` (утро/вечер) и `cron-radar-alerts.sh` раскомментированы; `getMe` = ok (`@NpzFuel_Bot`). Бот и канал `@NPZmap` оставлены для владельца (подписчиков не было). ВАЖНО: публичные кнопки Telegram на сайте (`index.html`, `radar.html`) остаются УБРАННЫМИ — канал/бот работают приватно, без публичной привязки к сайту. Отменяет строку про «отключение Telegram» из записи v1.4.12.
- 2026-07-07 — `v1.4.13` (по явному запросу владельца): убрана кнопка «Неточность» с `mailto:` на личную почту владельца из шапки карты (`index.html`). Почта больше не встречается ни в одном файле репозитория. Причина — деанон-риск.
- 2026-07-07 — `v1.4.12` (по явному запросу владельца): убраны ссылки на Telegram с сайта — кнопка канала `@NPZmap` (`index.html`) и баннер бота `@NpzFuel_Bot` + CSS (`radar.html`). На Hermes VPS отключена Telegram-публикация: токен `~/.npz-bot/token` деактивирован (→ `token.disabled-*`), закомментированы cron-строки `broadcast.py --briefing` (утро/вечер) и `cron-radar-alerts.sh`; сбор данных/`/news`/git-sync не тронуты (бэкап crontab: `/root/.crontab.backup-*`). Причина — снижение юридических/деанон-рисков; удаление самих канала и бота в Telegram — за владельцем.
- 2026-07-07 — `v1.3.0`: перестройка пайплайна публикаций (Fable 5). Единый рендер (`hermes/bot/render.py`) + единый дедуп (`hermes/bot/day_state.py`) для сводки/молнии/апдейта/БПЛА-алерта. Молния (`strike_pipeline.py`/`radar_publish.py`) переведена из dry-run в прод: TIER1 авто в канал, TIER2 владельцу с кнопками Опубликовать/Отклонить. `broadcast.py --update` — пополнение сводки (editMessageCaption/Text). Legacy editorial/diff-дайджест отключены из publish-путей (`NPZ_LEGACY=1` для ручного вызова). Найден и исправлен баг: `strike_pipeline.py --dry-run` персистил state — добавлен `--backfill` для безопасного включения молнии без спам-рассылки по 65-ударному беклогу. Полный диагноз: `docs/agents/audit-2026-07-07.md`; редполитика: `docs/npz-posting-style-v2.md`; миграция VPS-джобов: `docs/agents/jobs-migration-2026-07-07.md`.
- 2026-07-07 — incident: compact UI `v1.0.0` был опубликован прямым Vercel deploy, но не закреплён в `origin/main`; последующие Hermes/GitHub Actions production deploys перетёрли alias старой оболочкой. Решение: вернуть компактный UI как git-backed release, поднять версию, добавить UI-shell lock в `HERMES.md` и `.githooks/pre-commit`.
- 2026-06-11 — init проекта (Fable 5): каркас, фронт (Leaflet HUD), seed-датасет из ресёрча, скрипты cron-агентов (Haiku), vercel.json. Деплой на Vercel — в процессе.

- 2026-07-08 (Claude/Opus): единая шапка radar+sources; «Свежее»-чип на главной (build-nav); единый sitemap-генератор — gen-news зовёт seo/generate-sitemap.py (фикс затирания лендингов), /support исключён. Фронтенд-релиз (index.html) под ALLOW_FRONTEND_RELEASE=1.

- 2026-07-09 (Claude/Opus): новая страница /karta-bpla (tool-воронка на радар, кластер «карта бпла» ~126к/мес из XMLRiver); registry-driven меню/хаб/чип/sitemap обновлены. Фронтенд-релиз (index.html чип+дропдаун) под ALLOW_FRONTEND_RELEASE=1.
