node_modules/
.DS_Store
.vercel
agents/logs/*.log
*.log
.venv/
__pycache__/

# бэкапы правок, которые git-sync сбросил (см. agents/git-sync.sh) — локальные, не в репо
.claude/discarded/

# Мусор от агентов (01–02.08.2026: за сутки в корне скопился 21 такой файл и дважды
# заклинил git pull). Правило промптов «временные скрипты — только в /tmp» нарушается,
# поэтому подстилаем соломку. ВАЖНО: паттерны привязаны к КОРНЮ (ведущий /) — живые
# тесты agents/test_*.py и hermes/bot/test_*.py трогать нельзя, они отслеживаются.
*.bak-*
/debug*.py
/test_*.py
/check_*.py
/process_*.py
/fetch_*.py
/run_*.py
/extract_*.py
/crimea_*.py
# 09 и 11.08.2026 — тот же класс дважды: в корне и в agents/ появлялись update_*.py,
# update_rotation_*.jq от чужих сессий и держали дерево hermes грязным (git pull --rebase
# падает → синк встаёт у ВСЕХ агентов). Ловим и в корне, и в agents/.
/update_*.py
/update_*.jq
agents/update_*.py
agents/update_*.jq

# Рабочие каталоги worktree-агентов Claude Code — локальные, в репо им не место.
.claude/worktrees/
# 18.08.2026: префиксного /update_*.py не хватило — тем же мусором прилетел
# npz-history-crimea-update.py (суффикс, не префикс). Ловим и такую форму в корне.
/*-update.py
