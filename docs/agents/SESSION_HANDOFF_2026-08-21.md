# Handoff — npz-tactical-map — 2026-08-21

## Status
hermes-vps лежал 14 часов (20.08 вечер → 21.08 утро), поднят владельцем через панель
провайдера. Восстановление закрыто полностью: стэши разобраны, дыра в архиве за 20–21.08
догнана (5 ударов), обложки и сводки собраны, плашка «сбор прерван» снялась сама.
Неделя до этого ушла на подключение стражей, которые были написаны, но никем не вызывались.

## Read first (в порядке)
1. `docs/agents/incidents.md` — журнал инцидентов (открытых сейчас 0)
2. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо
3. Память проекта: `npz-hermes-outage-recovery` (чек-лист после падения),
   `npz-guards-written-but-never-wired`, `npz-scraped-pages-as-strikes`

🔴 Очередь `queue-2026-08-01-3day.md` закрыта полностью — новую надо составлять заново
(через `npz-pulse`).

## In-session decisions
- **Радар переживает падение hermes, архив ударов — нет.** `/api/radar-state` идёт через
  Vercel к внешнему источнику. При простое паниковать за карту не надо, догонять надо
  удары и `fuel-state`. (отклонено: failover на base-vps — там нет ни репо, ни `.hermes/.env`,
  разворачивать часами и потом ловить дубли публикаций)
- **Порядок догона обязателен:** удары → брифы/`gen-news.py` → обложки. `build-covers.py`
  правильно отказывается работать, пока нет лид-удара в архиве.
- **Мусор в данных ловим по ПОДПИСИ записи, а не по словам.** Дважды чуть не удалил
  настоящие удары: сначала по англоязычному тексту, потом по слабому `source_url`.
  Ссылочные признаки учитываются только вместе с отсутствием координат.
- **Аналитику страниц молча не переписываю.** На `/situaciya-s-benzinom` июльский тезис
  «ударов втрое меньше» опровергнут августом — раздел переписан под август по прямой
  просьбе владельца, а не по своей инициативе.

## Next step
🔴 Решение владельца по `casualties` в `strikes.json`: поле имеет ТРИ типа (`int`, `str`,
`dict`), 193 записи хранят свободный текст («1 погибший, 9 раненых»). Содержание верное,
но любой код, который станет складывать потери, сломается. Нужна миграция схемы — решить,
что канон (число + текст отдельно?), и переписать записи.

Затем по приоритету:
1. **Замеры:** литеральные дыры `/karta-bpla` — окно 25–28.08 (база в очереди 01–03.08);
   подкластер «пострадало» — база там же, окно прошло, померить задним числом.
2. **Монетизация — решение висит с 31.07.** Канал втрое конверсионнее VPN (14–26 кликов/сут
   против 0–6). VPN отключён 09.08, слот отдан каналу. `@npz_karta_bot` существует без кода.
3. **6 страниц-пустышек** (`/tenevoj-flot`, `/tankery-ekonomika-rf`, `/azhiotazh-na-azs`,
   `/benzin-na-trasse`, `/gde-dizel`, `/situaciya-s-benzinom` частично) — 0–2 визита за 30 дней.
   Кандидаты на склейку, нужно решение по 301-редиректам.
4. **Vercel ЭТАП 2** (свой домен + Cloudflare) — теперь плановая задача, не аварийная:
   команда на платном Pro, ~$34–51/мес, лимиты не жмут.
5. **Чужой джоб `logika-itp`** на hermes падает постоянно (нет playwright). НЕ чинить —
   он когда-то писал хардкодом в наш репо, сломанным безопаснее. Но он шумит в
   `executions.db` и маскирует наши падения: при разборе фильтровать по `job_id`.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-08-21.md`
2. `CLAUDE.md`

Затем прогони скилл npz-pulse и жди мою команду.
```
