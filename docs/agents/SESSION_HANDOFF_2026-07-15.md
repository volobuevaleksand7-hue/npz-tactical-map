# Handoff — npz-tactical-map — 2026-07-15

## Status
Волна 3 SEO — 3 из 4 задач закрыты, осталась задача 4; параллельно вскрыт инцидент затирания архивов данных.

## Read first (in order)
1. `docs/agents/tz-seo-wave3-2026-07.md` — ТЗ волны 3: 4 задачи, целевые запросы с показами/позициями, критерии приёмки, грабли публикации
2. auto-memory `npz-growth-backlog` — бэклог роста трафика
3. `CLAUDE.md` репо — правила проекта

## In-session decisions
- Задача 4 ТЗ УСТАРЕЛА в части линковки: `refineries.html` теперь ГЕНЕРИРУЕТСЯ (`agents/gen-refineries.py`, маркеры `<!-- GEN:table -->`, `GEN:regions`, `GEN:operators`, `GEN:working`). Ручные ссылки на карточки внутри GEN-блоков затрёт первый же прогон генератора → линковку встраивать В САМ генератор (авто-детект существования `/npz/<slug>.html`), а не руками в HTML.
- Проекции данных на страницах руками НЕ писать — только через генератор (паттерн `gen-refineries.py`). Причина: дважды за сутки статичный срез разошёлся с данными. Зафиксировано в `data/seo-topics.jsonl` у `/karta-bpla`.
- Задача 1 (`/karta-bpla`) опубликована в УРЕЗАННОМ виде (коммит 6a76bc01): вырезан статичный срез «6 регионов за 12-14.07» — строился на усечённом архиве (давал 6 регионов вместо 8), заявлял «по подтверждённым данным» при confidence=reported у 10 из 11 записей, и протухал за сутки. Оставлена долговечная часть: h2 «Мониторинг БПЛА по регионам и городам» + перелинковка на 9 существующих `/raketnaya-opasnost-<город>` + 2 FAQ (в видимом блоке и в JSON-LD) + кросс-линк `/volna-dronov`.
- Задачи 2 (`/refineries` операторы + «какие работают сейчас») и 3 (Капотня) закрыты АПСТРИМОМ параллельно (коммиты 7592bcf1, a2309833) — не переделывать. Задачу 2 апстрим сделал лучше: через генератор, который починил 12 расхождений по 6 заводам (счётчики 10/9/13 совпадали случайно).
- «нпз тверь» (272 показа, поз 9) — страницу НЕ делать: в `data/fuel-state.json` завода в Твери нет, ранжируемся случайно. Сначала фактологическая проверка, существует ли он.

## Next step
Задача 4 из ТЗ — 7 карточек `/npz/*` (Волгоградский, Туапсинский, Астраханский ГПЗ, Ангарский, Афипский, Новокуйбышевский, Сызранский), но линковку встроить в `agents/gen-refineries.py`.

## Parallel work (не дублировать)
Инцидент затирания архивов данных чинит ОТДЕЛЬНАЯ сессия. Факты: `strikes.json` затёрт 75→2 коммитом `844e802c` «health: watchdog 2026-07-12T23:02Z», восстановлен до 197 записей. Тот же класс бага у `fuel-voices` (0→96, коммит 21dbd22e). Корень: pre-commit гвард честно блокирует коммит коллектора, но усечённый файл остаётся в дереве VPS, и ежечасный watchdog (`git add data/` → `git-sync.sh`) коммитит его через 60 секунд. Гвард защищает коммит, а не файл.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. docs/agents/SESSION_HANDOFF_2026-07-15.md
2. docs/agents/tz-seo-wave3-2026-07.md
3. auto-memory: npz-growth-backlog
4. CLAUDE.md репо

Первая работа — задача 4 из ТЗ волны 3 (карточки /npz/*), с учётом того что
refineries.html теперь генерируемый. Жди мою команду.
```
