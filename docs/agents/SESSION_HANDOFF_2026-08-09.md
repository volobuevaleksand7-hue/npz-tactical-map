# Handoff — npz-tactical-map — 2026-08-09 (обновлён вечером)

## Status
Утренняя версия этого файла ставила три замера и решение по Екатеринбургу. Всё закрыто.
Сверх плана вскрылась и починена авария рассылки (подписчикам ушёл спам из 11 старых ударов)
и неделя битых обложек на `/news`. Три замера дали ответ, один реопт признан несработавшим и
заменён другой правкой в тот же день.

## Read first (в порядке)
1. `docs/agents/queue-2026-08-01-3day.md` — очередь; в `done:` каждого пункта лежат методики
   и базы замеров. Очередь 01–03.08 закрыта полностью, следующую надо составлять заново.
2. `data/seo-topics.jsonl` — границы интентов; `data/position-queries.json` — что мерит трекер
3. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо

## Сделано 09.08

**Авария рассылки (спам подписчикам).** 03:40–03:51 МСК в бота ушло 11 отдельных сообщений
про удары 28.07–01.08. Разбор по `/root/.hermes/cron/executions.db`:
- джобы молний были мертвы 33 часа (790 фейлов `can't open file` с 07.08 15:45);
- у контура BPL-бота файл состояния отстал (`seen=9` при 365 ударах) при 26 живых
  подписчиках — init-guard срабатывает, только если файла НЕТ, поэтому прогон поставил в
  очередь 4 224 сообщения, упёрся в таймаут 3600с и был убит ДО сохранения `seen`;
- следующий прогон начинал ту же рассылку с нуля — по кругу.

Починено тремя слоями в `hermes/bot/strike_alerts.py` (коммиты `4d21f4aeb`, `709b7b438`,
`f16fcd361`): группировка по времени СОБЫТИЯ (разрыв ≤2ч — одно компактное сообщение
списком, >2ч — отдельное), отсечка по свежести (`MAX_AGE_HOURS=36`), re-seed guard
(`RESEED_THRESHOLD=20`: непрочитанных больше — состояние рассинхронизировано, досеиваем без
рассылки). Тест `hermes/bot/test_strike_alerts_batch.py`. Реплей на реальном архиве:
38 ударов инцидента → 0 сообщений; без отсечки группировка даёт 11 вместо 38.

**Обложки: 6 файлов отдавали 404 неделю.** `optimize_covers` кладёт рядом с `cover-*.png`
лёгкий `cover-*.webp` (54 КБ против 202 КБ), `news.html` ссылается именно на webp — а в
`git add` у публикатора был только `*.png`. Файлы копились untracked на hermes: `/news`
показывал битые картинки И это же держало дерево грязным, роняя `git pull --rebase` всем
агентам (тот самый «мусор на hermes» из прошлого хендоффа). Коммит `c720063c3`: webp-сосед
стейджится в трёх местах (`hermes/publish-vps.sh`, `agents/summary-watchdog.py`,
`agents/wave-detect.py`), довезены 10 пропавших файлов, догнана навигация на двух отставших
страницах. Все обложки на проде отдают 200.

**Сторож поднят и обезврежен.** Джоб `NPZ WATCHDOG` (`fa4a26601b17`) лежал ~32 часа:
раннер Гермеса резолвит `script` относительно `/root/.hermes/scripts/` и отказывается
выходить за него, а в джобе стоял путь `npz-tactical-map/scripts/watchdog.sh`. Заведена
обёртка `/root/.hermes/scripts/npz-watchdog.sh`, джоб перенаправлен на неё (бэкап
`jobs.json.bak-20260809`). 🔴 Перед тем как поднимать, из `scripts/watchdog.sh` убран
`git stash push/pop` (коммит `a3e910b37`) — именно он плодил осиротевшие стэши, которые
рецидивно роняли git-sync всему флоту (30.07 публикация встала на 13–34 часа). Теперь
грязное дерево не прячется под стэш, а пропускает pull с громким сообщением. Прогнан
вручную: pull → `health: healthy | stale 0/11 | dead 0` → git-sync, стэшей не оставил.

## In-session decisions
- **Молния по Екатеринбургу (склад WB 07.08) НЕ отправлена, событие живёт только на карте.**
  Решение Серёги. (отклонено: слать молнию задним числом; отклонено: пунктом в сводке)
- **`/karta-bpla` перестали «усиливать занятое».** Замер показал, что реопт 27.07 не
  сработал: он бил по фразам, уже стоявшим на позициях 5–7. Вместо него закрыты литеральные
  дыры — фразы, которые ранжируются на голой релевантности и не встречались на странице
  ни разу.
- **Бренд конкурента не таргетим.** «детектор аэро карта бпла онлайн» — 596 показов, но
  CTR 1.8% на позиции 8.1 читается как лояльность к detector.aero, а не наш спрос.
- **Сторож, который ломает синк, хуже отсутствующего сторожа.** Поэтому `git stash` из
  watchdog убран, а не оставлен «как было, лишь бы работало».
- **Сравнивать недели, а не одинаковые дни недели.** Суббота 01.08 была аномально низкой,
  и сравнение «суббота к субботе» дало ложную картину (сайт «+37%», `/karta-bpla` «+121%»).
  По полным неделям сайт −2%, `/karta-bpla` +7%.

## Что показали цифры (для следующей очереди)
Сайт по неделям плоский: 24 746 → 24 233 визита (−2%). Прирост всех растущих страниц съела
одна: `/udar-po-skladu-ozon` 1 713 → 60 (−97%) — ушла новостная волна 27–31.07. То есть
органического роста за неделю не было, он держался на новостях.

Растут: `/karta-skladov-wildberries` +76% (574→1011), `/refineries` +10%, `/karta-bpla` +7%,
`/radar` +3%. WB-пара (чемпион + новая `/kakie-sklady-wildberries-ostalis`) +2% — новая
страница пока в основном перераспределяет трафик чемпиона, а не добавляет; судить рано,
плановый замер 13–16.08.

Формула, подтверждённая замерами: реопты картовых и объектных страниц дают −0.5…−0.7
позиции, реопт головной `/karta-bpla` не дал ничего. Вкладываться в первое.

## Next step
Очередь 01–03.08 закрыта полностью — нужна новая. По цифрам приоритеты такие:

1. **Замеры по расписанию.** Новая `/kakie-sklady-wildberries-ostalis` — 13–16.08;
   подкластер «пострадало» — 14–17.08; литеральные дыры `/karta-bpla` — 16–19.08.
   Базы всех трёх лежат в `done:` соответствующих пунктов очереди.
2. **Монетизация — решение висит с 31.07 (пункт B1).** Канал стабильно втрое конверсионнее
   VPN: 14–26 кликов/сут (0.39–0.69%) против 0–6 (0.06–0.14%), при этом бот заморожен и
   ведёт в никуда. `@npz_karta_bot` существует, но без кода. 🔴 Если делать под него алерты
   — не копировать дефолт `poll_bpl.py` (regions=all + напоминания 60 мин), это заморозило
   `@BPLAlert_bot` 15.07.
3. **3 метки не в том регионе:** Брянск 07.08 (≈167 км), Белгород 01.08 (≈93 км) и 25.07
   (≈59 км). Метод поиска — медиана по городу среди записей с 4+ точками.
4. **8 записей без `source_url`** (06–14.07, все `rumored`) — MiMo не смог, у Claude веб есть.
5. **22 протухшие страницы** (`healthcheck`) — это и есть пункт C2 в новой формулировке:
   тонкие страницы без спроса тянут кластер вниз, решение по каждой с цифрами.
6. Битая транслитерация в `data/strikes.json` («завод ZAO «Гидросталькonstruktsiya»») —
   отдана отдельной сессии.

## Чего делать НЕ надо
- **Не заводить страницы под Брянск и Крым**, хотя `npz-pulse` подсвечивает их как «страницы
  нет». По Брянску нет ни одной поисковой фразы даже на 100 показов, а по Крыму `/crimea`
  существует — матчер пульса просто не сводит «Республика Крым» с «Крым». Ложная тревога.
- **Не мерить `data/position-tracker.json` против старых снапшотов** — до 02.08 он находил
  3 запроса из 40. Мерить прямо из Вебмастера равными окнами.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-08-09.md`
2. `docs/agents/queue-2026-08-01-3day.md` (закрыта — нужна новая очередь)

Затем прогони скилл npz-pulse и жди мою команду.
```
