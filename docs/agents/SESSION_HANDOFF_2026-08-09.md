# Handoff — npz-tactical-map — 2026-08-09

## Status
Неделя ушла на аварии данных, а не на рост: архив ударов обнуляли ДВАЖДЫ (05.08 320→0,
06.08 336→0), плюс вскрылось, что удары попадали в архив без координат и что сборщик вообще
не искал удары по складам. Всё восстановлено и закрыто механизмами (архив 365 записей, прод
200). Открыты 3 замера с зафиксированной базой и решение по одной публикации.

## Read first (в порядке)
1. `docs/agents/queue-2026-08-01-3day.md` — очередь, в `done:` каждого пункта лежат базы замеров
2. `data/seo-topics.jsonl` — границы интентов кластера складов (9 живых страниц, легко
   каннибализировать); `data/position-queries.json` — что реально отслеживает трекер
3. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо

## In-session decisions
- **Защиту накопительных архивов дублировать в sweeper'е, не только в хуке.** pre-commit
  гарантированно работает на `git commit`, но НЕ на коммитах от `rebase --continue` — через
  это архив дважды уехал в прод под сообщениями соседних рутин. Страж стоит в `git-sync.sh`
  до коммита + fail-closed в pre-commit + сверка размеров в `healthcheck`.
- **Интент «какие склады остались» отдан новой странице** `/kakie-sklady-wildberries-ostalis`,
  блок на чемпионе сжат до 3 строк со ссылкой. Чемпион остаётся на «сколько».
  (отклонено: держать оба в полном виде — конкуренция с сильнейшей страницей сайта)
- **Числа в текст руками не писать никогда.** Трижды за неделю ловили протухшие счётчики;
  теперь `sync-warehouse-counts.py` + динамический description в генераторе чемпиона.
- **MiMo — штатный рабочий агент проекта** (`MIMO_TIER=sub`). Коммитит только в свой worktree,
  push после проверки цифрами: флейкает и переформатирует файлы целиком.
- **Молнию по Екатеринбургу НЕ публиковал** — жду решения Серёги (см. Next step).

## Next step
🔴 Решение Серёги по Екатеринбургу: удар по складу WB 07.08 добавлен на карту, но молния в
`@npz_karta_online` не отправлена (событие уже вчерашнее). Варианты: слать сейчас / дать
пунктом в утренней сводке / оставить только на карте.

Затем по приоритету:
1. **Замер `/karta-skladov-wildberries`** — окно 9–10.08, то есть СЕГОДНЯ. База: «карта складов
   вб в россии» 7,80 → 5,60; «склады вб на карте» 8,56 → 7,83 (трекер, равные 7-дн окна).
2. **3 метки не в том регионе:** Брянск 07.08 (≈167 км), Белгород 01.08 (≈93 км) и 25.07
   (≈59 км). Метод поиска — медиана по городу среди записей с 4+ точками.
3. **8 записей без `source_url`** (06–14.07, все `rumored`) — MiMo не смог, у Claude веб есть.
4. Замеры: новая страница 13–16.08, подкластер «пострадало» 14–17.08 — базы в реестре.
5. Мусор на hermes от чужой сессии: `agents/update_59_minimal.py`, `update_rotation_59.jq`,
   `update_rotation_pass.py` — грязное дерево там роняет `git pull` рутинам.
6. 🔴 Проверить, что стало с Vercel Pro Trial — истекал 08.08 08:18 МСК, решение «остаться на
   Pro, отключив Speed Insights + Observability» принято, но выполнено ли — не проверено.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-08-09.md`
2. `docs/agents/queue-2026-08-01-3day.md`

Затем прогони скилл npz-pulse и жди мою команду.
```
