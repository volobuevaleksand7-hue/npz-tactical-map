# Handoff — npz-tactical-map · VPN-монетизация — 2026-07-11

## Status
VPN-баннер V6 задеплоен на прод (v1.18.0, sw-кеш v18, коммит a298924). Тест запущен. Осталось: замер кликов + проверка кабинета hidemy.

## Read first (in order)
1. auto-memory `project_npz_vpn_monetization.md` — полное состояние теста (инжектится сама)
2. `docs/vpn-monetization-research-2026-07-10.md` + `...-codex-review-...md` — исследование + ревью
3. auto-memory `project_npz_tactical_map.md` — инфра сайта (репо/деплой/дата-агенты)
4. `CLAUDE.md` — правила проекта

## In-session decisions (ещё не всё в файлах)
- **Монетизация = только VPN** (крипта-совместимо с анонимностью). РСЯ/AdSense/ОСАГО/топливные карты отброшены — все требуют деанон при выплате (проверено агентами).
- **Яндекс за VPN НЕ понижает** (форумы пусты); риск только РКН — Серёга его принял («под расстрельной статьёй по контенту»).
- **Трафик ~95% Яндекс, не TG** (Метрика). Аудитория — массовые водители, конверсия в VPN вероятно низкая.
- **Баннер V6:** SVG-щит+моно-лейбл, тил, обе темы. **Текст без «оплата криптой»** (касса hidemy — СБП/карты/крипта).

## Next step
Создать Метрика-цель `vpn_click` (JS-событие) + через 2 дня свести клики (Vercel/Метрика/кабинет hidemy). Проверить в кабинете hidemy: выплата партнёру реально без KYC/крипта?

## Деплой-памятка
Код→прод: `ALLOW_FRONTEND_RELEASE=1 git commit` → `git fetch`+`pull --rebase` (дата-агенты пушут каждые ~10мин) → `git push` → `vercel deploy --prod --yes`. Стили менял → `python3 agents/build-nav.py` (штампует ?v) + бампни sw CACHE.

## First message
```
Продолжаю npz-tactical-map (VPN-монетизация). Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-07-11.md`
2. auto-memory `project_npz_vpn_monetization.md`

Жди мою команду.
```
