# Очередь работ на 27–29 июля 2026 (3 дня)

Составлено 27.07 по данным `npz-pulse` (Метрика + Вебмастер, окно 13–27.07).

🔴 **Протокол против дублей:** очередь видна всем сессиям флота. Прежде чем брать пункт —
проставь `claimed: <кто, дата-время МСК>` в строке пункта и запушь эту правку ПЕРВОЙ,
отдельным коммитом. Пункт без `claimed` считается свободным. Сделанный пункт — `done:` +
ссылка на коммит. Мы уже теряли время на параллельной работе над одним ТЗ.

## Обоснование приоритета

Приток спроса за неделю — 30 новых запросов, **все до одного** про склады Wildberries,
суммарно >15 000 показов, и все приходят на ОДНУ страницу `/skolko-skladov-wildberries-ozon`
(позиции 3.6–4.0). Это уже не один интент, а кластер из нескольких разных вопросов.
Разведение кластера на отдельные страницы — самый крупный и самый обоснованный рост.

Второй по дешевизне — объектные страницы `/npz/*`: 14 заводов из 33 без страниц, кластер
ранжируется (~783 показа/мес), генератор готов, а по двум заводам только что были удары.

## День 1 — 27 июля

- [x] **A1. Новая страница «сколько осталось складов Wildberries»** — claimed: sonnet-A1, 27.07 11:11 МСК
  Ключи: «сколько осталось складов вб» (418 показов / 170 кликов / CTR 41%), «сколько складов
  вб взорвали» (455), «сколько складов вайлдберриз пострадало» (674), «сколько складов вб в
  россии сгорело» (713). 🔴 Развести с `/skolko-skladov-wildberries-ozon` (там интент «сколько
  ВСЕГО складов») и `/ataki-na-sklady-wildberries-hronika` (хроника событий). Здесь интент —
  текущий остаток сети: сколько было, сколько выбито, сколько работает.
  **done: ОТМЕНЕНО как каннибализация, страница НЕ создана.** Живая `/skolko-skladov-wildberries-ozon`
  уже несёт FAQPage-блок с вопросами «Сколько складов Wildberries осталось?» и «Сколько складов
  поражено ударами?» — прямые синонимы всех 5 ключей A1 (осталось/сгорело/пострадало/взорвали).
  Она же и есть донор 30 новых запросов (позиции 3.6–4.0) — то есть Google уже признал эту
  страницу релевантной именно этим вопросам, вторая страница на тот же ответ рвала бы сигнал
  пополам, а не растила трафик. Альтернатива (не выполнена, следующему агенту): реопт этой
  страницы — данные в `data/warehouses.json` устарели на 24.07 (8 поражённых), по `strikes.json`
  подтверждены ещё Тверь (24.07), Симферополь (24.07), Екатеринбург (25.07), возможно СПб/Моск.
  шоссе (25.07, надо развести с уже учтёнными Шушарами) — но это пересекается с C3 («обновить
  WB-хронику 24–29.07», день 3), сначала синхронизировать там, потом перегенерить страницу
  `agents/gen-warehouses-page.py` и обновить `dateModified`.
- [ ] **A2. Объектные страницы двух НПЗ** — claimed: sonnet-A2, 27.07 11:12 МСК
  `/npz/antipinskij-npz` (Тюменская обл., статус partial, удар 25.07) и
  `/npz/novoshahtinskij-npz` (Ростовская обл., статус partial, удары по Ростову 25 и 27.07).
  Данные — из `data/fuel-state.json`, шаблон — существующие страницы `npz/*.html`.
- [ ] **A3. Дописать `/udary-po-energetike-kryma`** — claimed: haiku-A3, 27.07 11:13 МСК
  Добавить события 26.07: Таврическая ТЭЦ (Симферополь), Черноморнефтегаз (Черноморское).
  Страница вышла утром 27.07, эти удары в неё не попали. Обновить dateModified.

## День 2 — 28 июля

- [ ] **B1. Вторая страница WB-кластера** — claimed:
  Кандидаты по частотности: «самый большой склад вайлдберриз в россии» (108 показов, поз 8.5),
  «все склады вб» (102, поз 7.5), «склады вб» (943 показа, поз низкая). Сначала снять свежие
  цифры через `agents/almost-there.py`, потом решать — страница или секция.
- [ ] **B2. Региональные страницы: Свердловская и Ростовская области** — claimed:
  По обеим идут удары (Екатеринбург 24–25.07, Ростов 25 и 27.07), страниц нет вовсе.
  Механизм — `agents/gen-rocket-danger.py`, как для уже живых 11 регионов.
- [ ] **B3. Починить `radar-state.json` (stale_alive)** — claimed:
  `/radar` — 14% трафика сайта. 🔴 Известная причина в прошлом: флап внешнего radar-map.ru,
  редеплой НЕ лечит. Сначала диагностика источника, потом правка.

## День 3 — 29 июля

- [ ] **C1. Тюменская область + решение по кластеру портов** — claimed:
  Регион без страницы, удар 25.07 (Антипинский НПЗ). По портам (удар по порту Ростова 27.07,
  доля логистики в целях выросла с 3% до 29%) — сначала частотность, потом страница или секция
  в существующей. Не плодить страницу под единичный повод.
- [ ] **C2. Добор бэклога `/npz`** — claimed:
  Дозаполнить тонкую `/npz/taif-nk` (~121 показ/мес) + усилить секцию «самые крупные НПЗ»
  (~471 показ/мес, ранжируется глубоко).
- [ ] **C3. Обновить WB-хронику** ударами 24–29.07 — claimed:
  🔴 Только в существующую `/ataki-na-sklady-wildberries-hronika`, НЕ новой страницей.

## Вклинивание срочных новостей

Если приходит удар по объекту, которого нет в реестре, и тема крупная (НПЗ, склады
маркетплейсов, энергетика региона-миллионника) — она идёт ВПЕРЁД очереди: тема живёт 2–4 дня,
страница на третий день волны собирает остатки. Правило прежнее: если тема уже покрыта —
обновляем существующую страницу, а не заводим новую.

## Правила исполнения (общие для всех пунктов)

- Свой git worktree от `origin/main`, работа в общем дереве запрещена. `git add -u/-A` нельзя.
- Push: `GH_TOKEN=$(gh auth token -u volobuevaleksand7-hue) git push origin HEAD:main`,
  перед push — `git fetch origin main && git rebase FETCH_HEAD`. `gh auth switch` запрещён.
- Фронтенд-коммит с префиксом `ALLOW_FRONTEND_RELEASE=1`. Push сам запускает CI-деплой.
- Новая страница — запись в реестре ДО публикации, затем `python3 agents/build-nav.py`.
- 🔴 Тон строго нейтральный OSINT-русский (в pre-commit цензор). ПВО на картах не отмечать.
  Даты в UI по-русски. Факты только из `data/` — не выдумывать событий.
