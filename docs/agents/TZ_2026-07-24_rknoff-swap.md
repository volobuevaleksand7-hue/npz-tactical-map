# ТЗ — свап VPN-партнёрки hidemy → RKNoff + докрутка показа — 2026-07-24

Контекст: hidemy дал ~300 кликов / 0 продаж (990₽ — дорого). Новый партнёр — RKNoff,
Telegram-бот `https://t.me/rknoff_bot?start=ref-609952529`. ТГ-боты VPN обычно продают
за 120–190₽/мес → гипотеза: та же кликовая масса начнёт конвертить.
Исполнители — агенты. Оркестратор код не пишет.

Read first: `CLAUDE.md` (🔴 git-гигиена, аноним-аккаунт), `docs/agents/SESSION_HANDOFF_2026-07-24_vpn-swap.md`.

---

## T1 — свап рефа + текст под ТГ-бота (2 файла)

Новый REF: `https://t.me/rknoff_bot?start=ref-609952529`

### vpn-nudge.js
| Строка | Было | Стало |
|---|---|---|
| :7 | `var REF = 'https://hidemn.club/#6a514a15942d6'` | `var REF = 'https://t.me/rknoff_bot?start=ref-609952529'` |
| :39 | «Открыть можно через VPN — работает в РФ» | «VPN выдаёт Telegram-бот — минута, работает в РФ» |
| :40 | «…Открыть через VPN — работает в РФ» | «…VPN выдаёт Telegram-бот — минута, работает в РФ» |
| :48 | кнопка «→ Открыть источник» | «Получить VPN в Telegram →» |
| :134 | span «Западные СМИ по теме — через VPN» | «VPN — в Telegram-боте, минута» |
| :135 | кнопка «Открыть источник →» | «Получить VPN в Telegram →» |

### app.js
| Строка | Что |
|---|---|
| :30 | коммент `hidemy.name affiliate via Admitad` → `RKNoff TG-bot affiliate (ref-609952529)` |
| :35 | текст `.pp-vpn-b` «Открыть можно через VPN — работает в РФ» → как в vpn-nudge:39 (тексты держать идентичными) |
| :36 | href → новый REF; текст кнопки «→ Получить доступ через hidemy» → «Получить VPN в Telegram →»; `onclick` с `ym(...vpn_click)` СОХРАНИТЬ |

Не трогать: `rel="nofollow sponsored"`, `target="_blank"`, классы `.pp-vpn*`, RF_BLOCKED-списки (двойные копии — синхронны, не редактируются).
Правило текста: НЕ «открыть источник», НЕ «оплата криптой». Честно: кнопка ведёт в ТГ-бота за VPN.

## T2 — деплой (строго по протоколу handoff; только Claude-агент — токены)

1. `git worktree add -b rknoff-swap <dir> origin/main` — НЕ работать в общем дереве.
2. Правки T1 → `git add vpn-nudge.js app.js` (точечно! никаких -A/-u).
3. `ALLOW_FRONTEND_RELEASE=1 git commit` (аноним-identity, не реальное имя).
4. `python3 agents/build-nav.py` — перештамп `?v=` (оба файла в ASSET_RE, иначе кэш отдаст старый реф). Если build-nav изменил HTML — добавить эти файлы в коммит тоже точечным списком.
5. Проверить `sw.js`: если vpn-nudge.js/app.js в прекэше — бампнуть версию кэша.
6. Push: `GH_TOKEN=$(gh auth token -u volobuevaleksand7-hue) git push origin HEAD:main` (🔴 без `gh auth switch`).
7. Vercel: скопировать `.vercel/project.json` в worktree → `vercel --prod --yes` (git-автодеплой НЕ настроен).
8. `git worktree remove`.

## T3 — проверка прода (обязательно, скриншоты/грепы в отчёт)

- Попап карты (НПЗ с заблок-источником, напр. Кинеф/Meduza): новый href + новый текст кнопки.
- Статика (напр. /attacks): контекстная плашка — новый href + текст.
- Плавающий язычок на карте: карточка — новый href + текст.
- `curl -s https://npz-tactical-map.vercel.app/vpn-nudge.js | grep rknoff` и отсутствие `hidemn.club` где-либо: `grep -r hidemn` по проду-исходникам после деплоя = 0.

## T4 — расширение точек показа (ОТДЕЛЬНАЯ задача, после T1–T3)

Baseline есть (300 кликов/0 продаж) — мораторий Codex на расширение снят.
1. Боковые панели карты (лента ударов, «голоса», repair-строки): у заблок-источников показывать промо `srcHtml`-стилем — максимум одна плашка на панель, не спамить.
2. Тренд-страницы без заблок-ссылок сейчас получают слабую общую плашку. Как ранее сделали для attacks/karta-bpla/refineries: добавить РЕАЛЬНЫЕ ссылки на Meduza/Moscow Times (брать из существующих npz/*, news/* — не выдумывать) на: `zakrytye-azs`, `tenevoj-flot`, `udary-po-tankeram`, `skorost-remonta-npz`, `raketnaya-opasnost-*` (корневые). 🔴 Только статика вне генераторов; генераторные страницы (news/*, volna-dronov/*, refineries-волна) — правки ТОЛЬКО через генераторы, иначе сотрётся.
3. drafts/* НЕ трогать (не опубликованы, корневые копии уже покрыты).
4. Приоритизация: Серёга снимает топ-30 страниц из Метрики (110490245) — токена в репо нет.

## T5 — оператор (Серёга, руками, ~5 мин)

- Зайти в `rknoff_bot`: зафиксировать цену подписки и что `start=ref-609952529` засчитывает реферала (спросить у партнёрки, где смотреть статистику).
- Метрика: цель `vpn_click` уже существует — не трогать, классы кнопок не менялись.

## Замер успеха

Сравнение с baseline hidemy (≈1.5% CTR, 0/300 продаж): при тех же кликах ждём первые
продажи в течение 1–2 недель. Если и у RKNoff 0/300 — проблема не в цене, а в интенте
аудитории (Яндекс-поисковики за бензином, не за СМИ) → следующая гипотеза: офферы
не-VPN (обход через свой ресейл Marzban или вообще другая монетизация).
