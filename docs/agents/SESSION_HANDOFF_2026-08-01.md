# Handoff — npz-tactical-map — 2026-08-01

## Status
Очередь 27–29.07 выполнена целиком (20 новых страниц, 3 обоснованных отказа). Очередь 1–3.08:
дни 1–2 закрыты, день 3 (замеры C1/C2 + вес radar-state C3) ждёт своих дат. Заведён скилл
`npz-pulse` + утренний прогон очереди по крону. 🔴 На момент хэндофа висит живой инцидент — см.
Next step.

## Read first (в порядке)
1. `docs/agents/queue-2026-08-01-3day.md` — текущая очередь, в `done:` каждого пункта лежат
   базы для замеров (позиции/CTR) — без них замер 3–6.08 не с чем сравнивать
2. `~/.claude/skills/npz-pulse/SKILL.md` — как снимать расклад (трафик, приток фраз, удары
   против наличия страниц, монетизация); сборщик `scripts/pulse.py`, ~40 с
3. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо и границы интентов

## In-session decisions
- **Монетизация VPN — вкладываться не во что, пока нет статистики продаж.** Конверсия упала
  ~×10 (0,36–1,67% до 24.07 → 0,06–0,21% после), но причин три сразу: смена партнёрки, смена
  назначения (сайт → диплинк в бота) и честный CTA. Разделить их вклад по данным нельзя —
  прежняя формулировка «виноват честный CTA» была преждевременной. Потолок при 3–4 кликах/сут:
  ₽60–900/мес. 🔴 Действие за Серёгой: написать в `t.me/rknoff_sup` про кабинет и условия —
  пункт стоит в ТЗ с 24.07 и не выполнен. Спрос на алерты в боте выше VPN (данные по `/radar`).
- **Vercel: остаться на Pro, отключив Speed Insights + Observability.** Триал истекает
  **08.08 05:18 UTC = 08:18 МСК** (проверено через API). 🔴 Настоящая стена — не трафик, а
  Edge Requests: ~4,6 млн/мес против лимита Hobby 1 млн (4,5×). На Pro трафик и запросы стоят
  $0; счёт держат опциональные фичи. Домен + Cloudflare — отдельный этап на спад трафика, за
  неделю не успеть и `/api/radar-state` он не спасает. 🔴 Это опровергает запись предыдущего
  хендофа «трафика хватает с запасом».
- **Реопт `/karta-bpla` под «мониторинг/локатор» отложен до замера C1.** Каннибализации нет
  (проверено грепом). Слов «локатор» и «radar map» на сайте НЕТ вообще, но они уже ранжируются
  на поз 7,4–7,6 — дешёвый кандидат на реопт ПОСЛЕ замера.
- **Правило, спасшее трижды за очередь:** перед новой страницей грепать HTML живых страниц,
  включая FAQ-блоки, а не только реестр. Отменены: дубль под кластер тревог, «сколько осталось
  складов», страница под порты (нулевой спрос).

## Next step
🔴 Разобрать инцидент: `strikes.json` не обновлялся 15,4 ч, пайплайн пишет
`previously processed: 317, new: 0` (состояние ушло вперёд данных), в `agents/logs/cron.log` —
`error: cannot pull with rebase: You have unstaged changes`. Вчера то же лечилось ручным
перезапуском: `ssh hermes-vps`, `cd /root/npz-tactical-map`,
`NPZ_MODEL=claude-haiku-4-5-20251001 agents/run-agent.sh agents/update-prompt-newswatch.md strikes`.
После — прогнать `fetch-warehouses.py` + `gen-warehouses-page.py`, если приехали удары по складам.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-08-01.md`
2. `docs/agents/queue-2026-08-01-3day.md`

Затем прогони скилл npz-pulse и жди мою команду.
```
