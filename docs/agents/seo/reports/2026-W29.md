# SEO-отчёт 2026-W29 (13–19 июля 2026) — ПРОМЕЖУТОЧНЫЙ, замер реоптов

Второй отчёт цикла (PLAN.md шаг 1). Источники те же, что в `2026-W28.md`: Метрика (counter
110490245) + Вебмастер API (host npz-tactical-map.vercel.app).

🔴 **Неделя НЕ закрыта — отчёт снят 16.07 (четверг).** Метрика отдала 13–16.07 (4 дня),
**Вебмастер — только 13–14.07 (2 дня, задержка API ~2 суток)**. Поэтому всё сравнение —
**среднесуточное**, суммы за периоды разной длины несопоставимы. Полный W29 добрать после 19.07.

## KPI недели (среднесуточно, W28 → W29)

| Метрика | W28 (7 дн) | W29 (4 дн) | Δ |
|---|---|---|---|
| Визиты/сут (Метрика) | 477 | **2 166** | **×4,5** |
| Показы/сут (Вебмастер) | 21 416 | **25 166** | +18% |
| Клики/сут (Вебмастер) | 1 534 | **2 498** | **+63%** |
| CTR | 7,2% | **9,9%** | +2,7 п.п. |
| Отказы | 13,5% | 14,2% | +0,7 п.п. |
| Глубина | 1,53 | **1,74** | +14% |
| Время | 174 с | 135 с | −22% |

**Источники (W29):** Search **7 283** (84%) · Direct 915 · Internal 337 · Social 42 · Ad 39.
Органика из паритета с Internal (W28: 1 201 против 1 267 за период) стала явной доминантой.
Яндекс-моно как было, так и есть — задача «уйти от моно» из auto-memory `npz-growth-backlog` не сдвинута.

## 🚀 Реопт `/refineries` — сработал, это главный результат недели

Раскатан 12.07 — база отсчёта коммит `91fa7b3a` (12.07 14:30, «reopt хаба»); `613b9ad5` (14.07) и
`7592bcf1` (15.07) — follow-up. Замер — цель этого отчёта. Кластер «сколько НПЗ» был назначен главным
резервом W28 (поз 9 при ~6k показов).

| Запрос | W28 поз | W29 поз | Δ |
|---|---|---|---|
| сколько нпз в россии на сегодня | 8,4 | **4,4** | **+4,0** |
| сколько нпз в россии | 8,9 | **5,3** | **+3,6** |
| сколько в россии нпз | 9,2 | **5,3** | **+3,9** |
| сколько всего нпз в россии | 7,2 | **5,3** | +1,9 |
| сколько нпз в россии на 2026 год | 8,3 | **5,9** | +2,4 |

- **Кластер целиком:** показы 2 904 → 4 583/сут (+58%), клики **105 → 540/сут (+414%)**.
- **Страница:** 20,4 → **802,5 визита/сут (×39)**, отказы 12,8% (хорошие).

🔴 **Осторожно с атрибуцией.** Показы выросли на +58% в ОБОИХ кластерах (и в «карта бпла» тоже) —
это новостной фон: 13–15.07 массированные атаки (14.07 — 288 БПЛА по данным МО, Афипский +
Салават; 15.07 — удар по судам в Чёрном море). Фон дал показы, но **клики ×5 при +58% показов
даёт только позиция** — это заслуга реоптa, а не новостей.

## 🔧 Кандидаты на реопт (almost-there)

1. 🔴 **«карта бпла» — поз 7,8 · 3 108 показов · всего 74 клика (CTR 2,4%)**. Позиция просела
   с 6,9 (W28), CTR упал с 3,7%. **НО по `/karta-bpla` прошли ДВА реоптa — 15.07 (синонимы
   беспилотники/дроны/мониторинг) и 16.07 (карточка «беспилотная опасность» + FAQ), — а
   Вебмастер отдал только по 14.07. Эти цифры ДО обоих. Мерить в W30, сейчас выводов не
   делать и карту не трогать.**
2. **«сколько нпз в россии» — поз 5,3 · 4 494 показа.** Полдела сделано (было 9), до топ-3
   остался шаг. Дожать тем же приёмом: прямой ответ-число выше, разбивка по городам.
3. **«карта беспилотников» / «карта беспилотников онлайн» — поз 7,3–7,5 · ~770 показов.**
   ⛔ **Отдельную страницу НЕ заводить — это каннибализация.** Оба запроса уже в `keywords`
   у `/karta-bpla` (`seo-topics.jsonl`), синоним стоит в title и H1, а реопт 15.07 как раз
   и добавлял «беспилотники/дроны/мониторинг». Позиция 7,3 — это ДО реоптa (Вебмастер по
   14.07). Тот же незакрытый замер, что и п.1: ждём W30.
4. **«нпз россии» — поз 7,2 · 358 показов**, 21 клик. Общий интент, ведёт на `/refineries`.

## 🕳 Дыры и качество

- 🔴 **`/refineries` — трафик пришёл, но не удерживает.** Глубина **1,49** и время **100 с**
  против средних по сайту 1,74 / 135 с — худшие среди крупных страниц. 800 визитов/сут упираются
  в тупик. **Это следующая точка роста: перелинковка с `/npz/*` и картой** (карточки заводов как
  раз опубликованы сегодня — см. ниже).
- **`/karta-bpla`** — глубина **2,59 · 204 с**, лучшая вовлечённость на сайте (подтверждает W28).
- **`/npz/*`** — отказы **8,9–12,2%** (lukojl-norsi 8,9%, ryazanskij 9,0%, taif-nk 9,9%): гипотеза
  W28 про качество объектных страниц подтверждена на большем трафике. Кластер копать дальше.
- **`/news`** — глубина 3,10 · 240 с при 91 визите: по-прежнему самые глубокие сессии, показов мало.
- **`/vozmozhen-li-golod`** — из «тонких» W28 выросла до 118 визитов, отказы 10,2%. Не хоронить,
  как и договаривались.

## ⚙️ Системная заметка

- ✅ Трекер `data/yandex-positions.json` (засорение «обслуживание итп») — **закрыт 15.07**: причина
  была не в файле, а в чужом cron logika-itp, писавшем хардкодом в наш репо. Трекер удалён,
  позиции берём из Вебмастер API — как и в этом отчёте. См. auto-memory `npz-position-tracker-itp-contamination`.
- ⚠️ **16.07 в репозитории работали три стороны разом**: VPS-агент (UTC, github-noreply) выпустил
  3 карточки НПЗ в 05:19 и правку FAQ/JSON-LD `/refineries` в 05:03; Claude-сессия (+0300) — ещё
  4 карточки + перелинковку в 08:25; владелец — чистку бэклога в 08:18. Работа не продублирована,
  но перед запуском агентов сверяться с `git log` обязательно (см. auto-memory `npz-parallel-wave3-collision`).
- Сбор ударов переведён на inbox-схему (15.07): агент пишет `strikes-inbox.json`, архив правит
  `merge-strikes-inbox.py`. Первая ночь без усыхания архива, 197→201.
- Молнии включены 16.07 (крон 7,27,47) — до этого `strike_pipeline.py` не вызывал никто.
- 🔴 **Точка пересечения периметров инфра↔контент:** `/refineries` генерится из `fuel-state.json`
  (пишет инфра-сторона) с GEN-маркерами, проза — контент-сторона. 16.07 был рецидив дрейфа FAQ
  (`3a02f907`). Разъедется снова — сигнал между сторонами, `gen-refineries.py --check` его ловит.
- 🔴 **Граница `/karta-bpla` ↔ `/radar` зафиксирована в `seo-topics.jsonl`** (правка 16.07): на
  карту — всё «КАРТА <чего-то>» (тревог/воздушных тревог/ракетной опасности/беспилотной
  опасности), на радар — голая свежесть («бпла сегодня», «где сейчас тревога»). Сверяться
  перед любым новым «картовым» кандидатом, иначе каннибализация.

## Назначения оркестратору (следующий цикл)

1. **Замерить `/karta-bpla`** — данные будут в W30. По карте ДВА реоптa: `3deb3af6` (15.07 синонимы)
   и `4816e7cc` (16.07 «беспилотная опасность» + кластер тревог + FAQ). **Базу W30 брать от состояния
   ПОСЛЕ `4816e7cc`.** Это единственный незакрытый замер; до него реопт карты не трогать.
2. **1 реопт: `/refineries` — глубина, не позиции.** Позиции пришли (9 → 5,3), теперь 800
   визитов/сут утыкаются в глубину 1,49. Перелинковать на свежие `/npz/*` (7 карточек за сегодня)
   и на карту. 🔴 **Страница ГЕНЕРИТСЯ** (`agents/gen-refineries.py`, GEN-маркеры из `fuel-state.json`,
   `--check` ловит дрейф прозы) — перелинковку вносить в генератор, НЕ руками в HTML.
3. **1 новая страница — по итогам разведки, НЕ по «карта беспилотников»**: этот интент уже
   держит `/karta-bpla` (см. п.3 almost-there), дубль дал бы каннибализацию. Кандидаты — из
   бэклога: «самые крупные НПЗ России» или «какие НПЗ выведены из строя».
4. **Добрать полный W29 после 19.07** — этот отчёт снят на 4 днях Метрики и 2 днях Вебмастера.
5. Разведчику: `/npz/*` подтвердили низкие отказы на большом трафике — искать смежные интенты
   объектных страниц (по заводам, которых ещё нет) → `seo-backlog.jsonl`.
