# Handoff — npz-tactical-map — 2026-08-18

## Status
Сессия про инфраструктуру, не про рост. Архив ударов обнуляли ТРЕТИЙ раз (14.08, 403→0, трое
суток в проде) — восстановлен, 450 записей. Каталог Гермеса был удалён целиком, стек 12 дней
жил на удалённых инодах — восстановлен, перезапущен. Публичный бот не поднимается: токен мёртв.

## Read first (в порядке)
1. `docs/agents/SESSION_HANDOFF_2026-08-09.md` — замеры и SEO-решения, они не отменялись
2. `data/seo-topics.jsonl` — границы интентов; `data/position-queries.json` — что мерит трекер
3. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо

## Сделано 17–18.08
**Архив 25 → 422 (сейчас 450).** Обвал 14.08 сделал САМ САНИТАЙЗЕР: `sanitize-strikes` снёс
402 записи без date/city/confidence и сам сделал `git add` — уже ПОСЛЕ обоих стражей усыхания.
Сторож обвал заметил, но тревога прожила один прогон (следующий сравнил 0 с 0). Фиксы: откат
в pre-commit при обвале >10%, `archives_hwm` в healthcheck + пинг в Telegram.

**Гермес.** `/root/hermes-stack/` отсутствовал → падения `SSL_CERT_FILE ... missing CA bundle`
(27 прогонов, 6 джобов). Клонирован `github.com/NousResearch/hermes-agent` → v0.20.2, venv
пересобран. NEWSWATCH: `failed 05:00 → completed 06:28`, за час 12 прогонов и 0 SSL-ошибок.
Диск 92% → 88% (2.4 ГБ). Бэкап состояния: `/root/hermes-state-backup-20260818-0553.db.gz`.
`hermes-dashboard` (HTTP 200 через 90 с — пересобирал web UI) и `hermes-gateway` на новом
рантайме, крон рестарт пережил.

## In-session decisions
- **Containerd 2.0 ГБ не трогать** — живые контейнеры чужих проектов (Fundament21, demo-боты).
- **`incoming/2662` в tg-recon не удалял.** `fetch_inbox.py` тянет вложение ~700 МБ из входящих
  @ramassist ЦИКЛОМ: растёт до 681 МБ, сбрасывается, качается заново. Чужой проект; чинить
  логику (пропуск скачанного + лимит размера), а не место.
- **Боты перезапущены ради фиксов рассылки от 09.08** — процессы жили с 05.08, без
  `MAX_AGE_HOURS`/`RESEED_THRESHOLD`. Проверено ДО: архив 450, `seen` 469, непрочитанных 0.

## Next step
🔴 **Решение по публичному боту.** `npz-bpl-bot` (@BPLAlert_bot) падает с
`telegram.error.InvalidToken` — токен отклонён сервером, бот заморожен за спам с 15.07.
Сервис ОСТАНОВЛЕН после 10 попыток; подписчикам ничего не ушло, `seen` не сдвинулся.
Варианты: поднимать `@npz_karta_bot` (существует, кода нет) либо закрыть направление.
🔴 НЕ копировать дефолт `poll_bpl.py` (regions=all + 60 мин) — он и заморозил прошлого бота.
`npz-fuel-bot` (приватный) перезапущен штатно и работает.

Дальше по приоритету:
1. Сторож свободного места на VPS — 88%, и никто об этом не кричит.
2. Замеры (сроки прошли, снимать сейчас): `/kakie-sklady-wildberries-ostalis`, подкластер
   «пострадало», литеральные дыры `/karta-bpla`. Базы — в `done:` очереди 01–03.08.
3. 3 метки не в том регионе: Брянск 07.08 (≈167 км), Белгород 01.08 (≈93 км), 25.07 (≈59 км).
4. 8 записей без `source_url` (06–14.07, все `rumored`) — у Claude веб есть, у MiMo не было.
5. 23 протухшие страницы (`/deficit`, `/crisis`, `/situaciya-s-benzinom`, 24–26 дней).

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-08-18.md`
2. `docs/agents/SESSION_HANDOFF_2026-08-09.md`

Затем прогони скилл npz-status и жди мою команду.
```
