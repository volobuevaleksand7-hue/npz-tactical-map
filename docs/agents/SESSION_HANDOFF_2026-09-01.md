# Handoff — npz-tactical-map — 2026-09-01

## Status
Проект стоял 40 часов (29.08–01.09) — упёрлись в НЕДЕЛЬНЫЙ лимит Claude на аккаунте VPS,
не наша поломка. Лимит снят, флот догнан: 12 из 12 агентов отбились, health = healthy.
Codex на hermes был разлогинен (`token_revoked`) — восстановлен, обе машины проверены.

## Read first (в порядке)
1. `docs/agents/incidents.md` — журнал инцидентов
2. `CLAUDE.md` + `docs/seo-playbook.md` — правила репо
3. Память проекта: `npz-strike-improves-refinery-bug`, `npz-retiring-a-published-page`

## In-session decisions
- **Codex на VPS чинится переносом `~/.codex/auth.json` с Мака.** Токен отзывается сам при
  ротации (владелец ничего не убирал). Перенёс со снятием бэкапа, ОБЕ машины потом проверены
  прямым `image_gen` — взаимного гашения нет. (отклонено: `codex login --with-api-key` — ключа
  OpenAI нет ни на одной машине)
- 🔴 **`codex login status` — НЕ признак живости.** На hermes он отвечал «Logged in using
  ChatGPT», пока каждый вызов падал с `token_revoked`. Именно это дважды увело меня в
  «out of credits». Гейт на нём убран из `covers-mac-catchup.sh`; проверять только вызовом.
- 🔴 **Вес файла обложки больше НЕ отличает фото от заглушки.** Раньше работало (150–320 КБ =
  фото, 60–80 = типографика), сейчас все 92 весят >100 КБ, а дефекты есть. Только глазами.
- **Обложки догоняет Мак по расписанию** — `hermes/scripts/covers-mac-catchup.sh`, launchd
  `ru.npz.covers-catchup` (10:30/22:30). Окно 7 дней, НЕ `--missing`: голый `--missing` полез
  в июнь и намолотил 10 дат за 10 минут. Теперь это страховка, раз VPS-Codex ожил.
- **`sync-npz-counts --check` не покрывает аналитические блоки.** Он говорил «всё совпадает»,
  пока `/skorost-remonta-npz` три недели держала медиану и рекорд простоя на 11 августа.

## Next step
🔴 **Доделать обложки.** Агент `a0ebf98ae9b6decb1` классифицирует все 92 глазами и был
перезапущен после того, как встал на полпути. Пришло 3 партии из 6: 18.06–03.07 чисто,
21.08–01.09 чисто, **10.07 BAD — в картинку впечатана дата «2024-07-10»**. Ждут разбора
партии по началу июня, второй половине июля, первой половине августа. Найденные —
перевыпустить по одной дате: `NPZ_COVER_BACKENDS=codex-local python3
hermes/scripts/build-covers.py --dates <дата>`, параллельно НЕ запускать.

Затем по приоритету:
1. **Блокировка по дате в `build-covers.py`** — временный файл `~/.hermes/covers-tmp/raw-<дата>.png`
   один на дату, параллельные прогоны дерутся и дают ЛОЖНЫЙ GENFAIL (принял это за флейк Codex).
2. **Повтор в `summary-watchdog.py`** — сейчас пишет «долг» с первой неудачи, хотя вторая
   попытка обычно проходит.
3. **Два завода без источника:** `achinsk` и `angarsk` имеют пустой `source_url`. Страж
   `gen-npz-status-page.py` их не ловит (они `operational`), но при первом же ударе генератор
   упадёт — ровно так топ-страница `/rabotayut-li-npz-rossii` молча стояла 4 дня.
4. **Конверсия канала** просела: 0.2–0.5% против 0.75% в период, когда VPN-язычок был выключен.
   Нужен разрез по страницам входа и решение владельца, а не код.

## First message
```
Продолжаю npz-tactical-map. Не начинай пока не скажу.

Прочитай:
1. `docs/agents/SESSION_HANDOFF_2026-09-01.md`
2. `CLAUDE.md`

Затем проверь статус агента по обложкам и жди мою команду.
```
