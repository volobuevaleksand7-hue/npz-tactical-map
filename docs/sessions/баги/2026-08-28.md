# Баги — 2026-08-28

Проект: НПЗ — тактическая карта (`npz-tactical-map`)
Чат: `bcf790dd-6cc9-4295-bdc7-c16228030357` · Видимость: project · Тип: agent
Участники: —

---

## 08:03:04 UTC — Owner (web)

опять нету изображения! ты должен делать по шаблону! и там за ошибки? агенты работают?

[Вложение: uploads/paste-1787904147502-052f5cbc4959.png]

## 08:03:10 UTC — агент (agent)

Разобрался по обоим вопросам, но чинить пришлось бы на hermes — а с этой машины до него нет доступа (`hermes-vps` не резолвится, порт 22 по IP закрыт). Поэтому ниже — диагноз по данным из репозитория, он однозначный.

**1. Публикация действительно встала — алерт не ложный.**
Последний коммит агентов в origin — 07:10Z (10:10 МСК), сейчас 11:03 МСК, то есть ~53 минуты тишины при норме «радар каждые 10 минут». Ровно через минуту после последнего агентского коммита, в 07:11Z, в main лёг ручной коммит про SEO складов. Это в точности сценарий инцидента 29 июля: чужой пуш → на hermes встал rebase → `git-sync` правильно отказывается пушить, и флот молчит. `data/health.json` при этом показывает `healthy`, но он сам от 07:10Z — то есть watchdog замер вместе с остальными, ему верить нельзя.

**2. Обложки у утренней сводки не было по времени, а не по «забыл».**
Сводка уходит в 05:00Z. Обложка за 28 августа (`assets/cover-2026-08-28.png`) появилась только в 05:15Z — её досгенерил watchdog. То есть в момент отправки своей картинки ещё не существовало, а запасной вариант (взять вчерашнюю) не сработал, и пост ушёл текстом. У молнии в 09:00 обложка уже была — она вышла после генерации. Это системная дыра: генератор обложек по расписанию ходит позже брифинга, а сам брифинг зависит от того, успел ли предыдущий проход собрать архив новостей за сегодняшнюю дату.

**Что нужно от тебя, чтобы я доделал:** доступ к hermes (запустить меня с Mac, где есть алиас `hermes-vps`, или дать ключ сюда). Там надо снять затык — посмотреть `git stash list`, `tail agents/logs/cron.log` и конфликт-маркеры в `data/`, вернуть пуш. И отдельно я бы поправил порядок в утренней сводке, чтобы обложка гарантированно строилась до отправки, а не после — сейчас это лотерея каждое утро.

## 08:32:16 UTC — Owner (web)

Агент «Баги» может теперь повторить попытку и налететь на уже сделанное. Скажи ему перечитать состояние: публикация разблокирована, publish-vps.sh уже пофикшен и запушен (0389cd2e7).

## 08:32:19 UTC — агент (agent)

**Для агента «Баги»:**

Перечитай текущее состояние перед следующей попыткой — публикация уже разблокирована. Скрипт `publish-vps.sh` пофиксен и запушен в `0389cd2e7`. Повторяй с этого коммита, не трогай скрипт заново.
