# Сводки — 2026-09-07

Проект: НПЗ — тактическая карта (`npz-tactical-map`)
Чат: `c8d5e761-f4a3-4374-ab9d-74f3e151a81e` · Видимость: project · Тип: agent
Участники: —

---

## 05:06:54 UTC — Owner (web)

что за дым из трубы - подкорректируй генерацию фото

[Вложение: uploads/paste-1788757600324-2fedf2a1df25.png]

## 05:07:00 UTC — Сводки (agent)

Причина понятна: в промпте генератора для НПЗ было буквально написано «столб дыма **над** нефтезаводом» — без указания, откуда он идёт. Модель и рисовала облако, висящее в небе отдельно от труб, как на вашей картинке.

**Что поправил** — тексты промптов генерации обложек (сам код генерации, бэкенды и подписи не трогал):

Дым теперь всегда привязан к видимому источнику: у НПЗ — шлейф из конкретной трубы, основание примыкает к устью; у подстанции — от повреждённого оборудования на земле; у горящего танкера — от палубы судна; у склада — от кровли и очага пожара. Плюс во все промпты добавлено общее правило физики дыма (расширяется, сносится ветром, не отрывается от источника) и прямой запрет: «без парящих в небе отдельных клубов дыма». То же продублировано в инструкцию для Codex и в резервный генератор через OpenRouter.

**Важно:** проверить это глазами можно только на следующей сгенерированной обложке — я правил текст запроса, а саму картинку не перегенерировал (это тратит кредиты Codex и делается штатным прогоном). Старые уже выпущенные обложки остались как есть.

<details>
<summary>Технические детали</summary>

Файлы: `hermes/scripts/build-covers.py` (константа `SMOKE_RULE`, сцены в `meta_for()`, инструкция в `_codex_instr()`), `hermes/gen-cover-openrouter.py` (`build_prompt()`), запись в `AGENT_ACTIVITY.md`.

Коммиты `6cffb4b15`, `34b4b9f03` в ветке задания, identity `npz-agent`, без следов инструмента. `data/` не тронут, дерево чистое, `py_compile` зелёный, итоговые промпты по всем типам событий распечатаны и проверены на склейку.

Побочно найден предсуществующий баг (не мой и не чинил): в `meta_for()` в ветке очередей на АЗС переменная `lead` остаётся `None` и следующая строка падает с `AttributeError`.
</details>

Рекомендации:
1. Перегенерируй обложку последней сводки через build-covers.py и покажи результат.
2. Почини баг с AttributeError в ветке очередей на АЗС в meta_for().
3. Пройдись по обложкам за последнюю неделю и найди те, где дым оторван от источника.
