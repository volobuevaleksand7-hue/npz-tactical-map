# Аналитик — тест переноса — 2026-09-01

Проект: НПЗ — тактическая карта (`npz-tactical-map`)
Чат: `3a3737ae-f10b-42df-a19a-d292b2fa3fc7` · Видимость: project · Тип: agent
Участники: —

---

## 19:36:56 UTC — Owner (web)

Диагностика переноса, ничего не меняй и не коммить. Ответь коротко, по пунктам:
1) Перечисли слаги скиллов, которые тебе СЕЙЧАС доступны (посмотри фактически, не по памяти).
2) Виден ли тебе проектный скилл с префиксом npz-? Если да — какие именно.
3) Есть ли в репозитории каталог memory/ и что в memory/index.md, если он есть.
4) Одной строкой: кто ты по роли и чего тебе делать НЕЛЬЗЯ.

## 19:37:02 UTC — Аналитик (agent)

Проверил репозиторий этого воркtree напрямую, не по памяти. Каталога `memory/` в проекте нет вообще.

**1) Доступные мне сейчас скиллы** (полный список из системного реестра): `npz-map-refresh`, `npz-pulse`, `npz-status`, а также общие/платформенные — `ramos-armory:cro-methodology`, `ramos-armory:pain-driven-research`, `ramos-armory:research`, `ramos-armory:seo-optimizer`, `ramos-armory:website-intelligence`, `dataviz`, `update-config`, `keybindings-help`, `simplify`, `fewer-permission-prompts`, `loop`, `schedule`, `claude-api`, `run`, `init`, `review`, `security-review`.

**2) npz-скиллы видны** — все три: `npz-map-refresh`, `npz-pulse`, `npz-status`. Лежат физически в `.claude/skills/` этого воркtree (проверил `ls`, не по памяти). Перенос удался — раньше эти агент не видел.

**3) `memory/`** — такого каталога в репозитории нет (проверил `find` по корню воркtree — не найден). Значит `memory/index.md` тоже отсутствует. Вся накопленная память проекта на данный момент — только то, что прописано в `CLAUDE.md`.

**4) Роль:** я — аналитик роста «Топливного фронта РФ»: превращаю трафик/аналитику/удары в список задач; мне нельзя писать готовые тексты статей, трогать вёрстку сайта и файлы в `data/`.

Рекомендации:
1. Запусти /npz-pulse
