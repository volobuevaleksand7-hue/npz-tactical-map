# Статьи — тест переноса — 2026-09-01

Проект: НПЗ — тактическая карта (`npz-tactical-map`)
Чат: `b658004a-8ccc-410c-91a1-50a8fb825c14` · Видимость: project · Тип: agent
Участники: —

---

## 19:36:56 UTC — Owner (web)

Диагностика переноса, ничего не меняй и не коммить. Ответь коротко, по пунктам:
1) Перечисли слаги скиллов, которые тебе СЕЙЧАС доступны (посмотри фактически, не по памяти).
2) Виден ли тебе проектный скилл с префиксом npz-? Если да — какие именно.
3) Есть ли в репозитории каталог memory/ и что в memory/index.md, если он есть.
4) Одной строкой: кто ты по роли и чего тебе делать НЕЛЬЗЯ.

## 19:37:03 UTC — Статьи (agent)

**Диагностика переноса — по пунктам:**

**1) Доступные мне сейчас скиллы** (полный список из системного листинга):
npz-map-refresh, npz-pulse, npz-status, ramos-armory:copywriting, ramos-armory:humanizer, ramos-armory:seo-launch-resonance, ramos-armory:seo-optimizer, dataviz, update-config, keybindings-help, simplify, fewer-permission-prompts, loop, schedule, claude-api, run, init, review, security-review.

**2) Проектные скиллы с префиксом npz-** — видны, три штуки: `npz-map-refresh`, `npz-pulse`, `npz-status`. Физически лежат в `.claude/skills/` этого воркtree (подтвердил через `find`) — перенос сработал.

**3) Каталог `memory/` в репозитории проекта** — не найден вообще (ни в корне воркtree, ни где-либо ещё). Есть отдельный служебный каталог памяти RamOS-агента (`/home/ramos/.claude/projects/.../memory/`), но он **пустой** — ни `index.md`, ни каких-либо листиков памяти там нет. То есть накопленной проектной памяти (в смысле авто-memory системы) сейчас нет, есть только то, что записано в `CLAUDE.md` (корневом и в этом воркtree).

**4) Роль и ограничения** — я автор SEO-статей для «Топливного фронта РФ»; мне нельзя: трогать `data/` (это Гермес), править вёрстку/стили (это Сайтодел), самому решать, какую страницу писать (это Аналитик), и заводить новую страницу без предварительной записи в словаре публикатора.

<details>
Проверял: `find` по воркtree на `memory*`, `.claude/skills`, `index.md`; листинг корня репозитория; содержимое каталога памяти агента RamOS. Публикаторский словарь как отдельный файл в ходе этой диагностики не искал — задача была только про перенос скиллов/памяти.
</details>

Рекомендации:
1. Найди и покажи мне файл словаря публикатора, из которого берутся слаги новых страниц.
